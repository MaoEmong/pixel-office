# 결정 기록 (append-only)

형식: `D-## · 날짜 · 제목` / 맥락 / 결정 / 버린 대안 / 결과·근거. 뒤집으면 새 번호로 적고 옛 항목에 "→ D-##로 대체" 표시.

## 목차 (T31 에서 추가 — 본문은 **append 순서** 그대로고 이 표만 번호 순이다)

| # | 날짜 | 한 줄 | 태스크 |
|---|---|---|---|
| [D-01](#d-01--2026-09-14--연동-방식-실제-cli-tui를-pty로-띄운다) | 09-14 | 연동은 실제 CLI TUI 를 pty 로 띄워 타이핑 — Agent SDK 가 아니다 | 설계 |
| [D-02](#d-02--2026-09-14--데몬은-flutter와-분리된-상주-프로세스) | 09-14 | 데몬은 Flutter 와 분리된 상주 프로세스 | 설계 |
| [D-03](#d-03--2026-09-14--hook-명령은-node-스크립트-하나로-멤버-식별은-환경변수) | 09-14 | hook 명령은 Node 스크립트 하나, 멤버 식별은 `PIXEL_MEMBER` | T01·T03 |
| [D-04](#d-04--2026-09-14--허가질문은-hook-결정-반환으로-키-입력은-다이얼로그-통과-전용) | 09-14 | 허가·질문은 hook 결정 반환, 키 입력은 다이얼로그 통과 전용 | T03·T04 |
| [D-05](#d-05--2026-09-14--멤버-지시문은-sessionstart-hook-additionalcontext로-주입) | 09-14 | 멤버 지시문은 `SessionStart` `additionalContext` 로 주입 | T04·T26 |
| [D-06](#d-06--2026-09-14--팀-구조-팀장팀원-2단-팀장--오케스트레이터) | 09-14 | 팀 구조 2단(팀장/팀원) — **D-32 로 대체** | T24 |
| [D-07](#d-07--2026-09-14--데몬-재시작--전원-resume) | 09-14 | 데몬 재시작 = 전원 `--resume` (D-17 이 일부 정정) | T09 |
| [D-08](#d-08--2026-09-14--데몬-기술-스택) | 09-14 | 데몬 기술 스택(TS·Node 24·node:sqlite·ws·node-pty·xterm) | T00 |
| [D-09](#d-09--2026-09-14--m1-사무실-렌더링은-custompainter-flame은-m6에서-재검토) | 09-14 | 사무실은 CustomPainter, Flame 은 M6 재검토 | T12 |
| [D-10](#d-10--2026-09-14--문서화-규칙) | 09-14 | 문서화 규칙(태스크 1 = worklog 1, 결정은 여기 append) | 공통 |
| [D-11](#d-11--2026-09-14--hook-보류-타임아웃은-deny가-아니라-pass-through) | 09-14 | hook 보류 타임아웃은 deny 가 아니라 pass-through(`{}`) | T03 |
| [D-12](#d-12--2026-09-14--node-pty-conpty-자연-종료-시-내부-리소스를-직접-해제) | 09-14 | node-pty ConPTY 자연 종료 시 내부 리소스 직접 해제 | T01 |
| [D-13](#d-13--2026-09-14--tui-패턴-json에-verified-플래그) | 09-14 | TUI 패턴 JSON 에 `verified` 플래그 | T02·T21 |
| [D-14](#d-14--2026-09-14--sessionend-reason-clearresume은-프로세스-종료가-아니다) | 09-14 | `SessionEnd` `clear`/`resume` 은 프로세스 종료가 아니다 | T04 |
| [D-15](#d-15--2026-09-14--hook-보류가-결정-없이-끝나면-error-이벤트--재지시-필요) | 09-14 | 결정 없이 끝난 보류 → `error` + "재지시 필요" | T03·T18 |
| [D-16](#d-16--2026-09-15--세션-hooks-timeout은-86400초-만료-시-tui-프롬프트가-폴백) | 09-15 | 세션 hooks `timeout` 은 86400초 | T10·T30 |
| [D-17](#d-17--2026-09-15--데몬-강제-종료-시-살아남는-cli-자식은-재시작-때-정리reap한다--d-07-일부-정정) | 09-15 | 강제 종료 때 살아남는 CLI 자식은 재시작이 reap | T09 |
| [D-18](#d-18--2026-09-15--resume-실패-폴백은-1회-session_id를-비운다) | 09-15 | resume 실패 폴백은 1회, `session_id` 를 비운다 | T09 |
| [D-19](#d-19--2026-09-15--질문-pending의-tuiask_user-구분은-payloadtool_input-유무) | 09-15 | 질문 pending 의 TUI/`ask_user` 구분은 `tool_input` 유무 | T17 |
| [D-20](#d-20--2026-09-15--resumed-요약은-쓰기-전에-스냅샷) | 09-15 | `[RESUMED]` 요약은 쓰기 전에 스냅샷 | T09 |
| [D-21](#d-21--2026-09-15--재접속-시-이벤트-링은-eventsquery로-백필) | 09-15 | 재접속 이벤트 링은 `events.query` 로 백필 | T13 |
| [D-22](#d-22--2026-09-16--teamtools-mcp-도구-호출mcp__team__은-데몬이-자동-허가) | 09-16 | `mcp__team__*` 도구 호출은 데몬이 자동 허가 | T19 |
| [D-23](#d-23--2026-09-16--codex-종료는-ctrlc-1회-후-확인-tui-승인-프롬프트는-자동-통과-대상이-아님) | 09-16 | Codex 종료는 Ctrl+C 1회 + 확인, 승인 프롬프트는 자동 통과 금지 | T20·T21 |
| [D-24](#d-24--2026-09-16--codex-부팅-완료는-화면prompt-ready으로-판정-sessionstart는-첫-프롬프트-때-온다) | 09-16 | Codex 부팅 완료는 화면(prompt-ready)으로 판정 | T20 |
| [D-25](#d-25--2026-09-16--턴-종료-hook-이-없으면-화면으로-idle-을-판정한다idlesummaryscreen-idle) | 09-16 | 턴 종료 hook 이 없으면 화면으로 idle 판정 | T20·T23 |
| [D-26](#d-26--2026-09-16--approval-prompt-은-통과-가-아니라-막힘-이다--dialogpassed-대신-blockeddialogblocked) | 09-16 | `approval-prompt` 은 "통과" 가 아니라 "막힘" | T21 |
| [D-27](#d-27--2026-09-16--팀-셸-뮤텍스는-pretooluse-에서-잡되-읽기-전용-명령은-잡지-않는다) | 09-16 | 셸 뮤텍스는 `PreToolUse` 에서, 읽기 전용은 안 잡는다 | T27 |
| [D-28](#d-28--2026-09-16--살아-있는-팀장-판정과-teamcreate-반환-변경) | 09-16 | "살아 있는 팀장" 판정과 `team.create` 반환 | T24 |
| [D-29](#d-29--2026-09-16--보고를-기다리는-턴-종료는-끝난-작업이-아니다--위임-task-의-승격도-팀장-버퍼를-탄다) | 09-16 | 보고를 기다리는 턴 종료는 "끝난 작업" 이 아니다 | T25 |
| [D-30](#d-30--2026-09-16--주입되는-것은-파일-이-아니라-프리앰블--유효-지시문--기본-템플릿은-저장하지-않고-매번-계산한다) | 09-16 | 주입은 파일이 아니라 프리앰블 + 유효 지시문 | T26b |
| [D-31](#d-31--2026-09-16--후처리는-표-하나-파생-상태는-함수-하나--memberrestart-만-ask_user-예외를-못-받는다) | 09-16 | 후처리는 표 하나, 파생 상태는 함수 하나 (D-36 이 정정) | T28 |
| [D-32](#d-32--2026-09-16--직무-체계-rev-3-나--부장--팀장--팀원-트리) | 09-16 | **직무 체계 rev 3: 나 → 부장 → 팀장 → 팀원 트리** | M4b |
| [D-33](#d-33--2026-09-16--트리-스키마의-소유-관계-taskevent셸-락은-부서-소유-부장은-팀에-속하지-않는다) | 09-16 | task·event·셸 락은 **부서** 소유 | T34 |
| [D-34](#d-34--2026-09-16--없어진-사용자-기능은-지우지-않고-forcetrue-뒤로-숨긴다) | 09-16 | 없어진 사용자 기능은 `force:true` 뒤로 | T34 |
| [D-35](#d-35--2026-09-16--teamdelete-는-보고-이력을-남기고-departmentdelete-는-task-까지-지운다) | 09-16 | `team.delete` 는 이력 보존, `department.delete` 는 task 까지 | T36 |
| [D-36](#d-36--2026-09-16--hook-보류-취소와-pending-만료를-갈라-memberrestart-도-턴-종료-질문을-살린다--d-31-딸린-결정-정정) | 09-16 | 보류 취소 ↔ pending 만료 분리 → `restart` 가 질문을 살린다 | T36 |
| [D-37](#d-37--2026-09-16--ask_parent-는-ask_user-와-같은-pending-한-종류로-두고-답하는-길을-둘로-연다) | 09-16 | `ask_parent` 는 `ask_user` 와 같은 pending 한 종류 | T35 |
| [D-38](#d-38--2026-09-16--마이그레이션이-남긴-pending-fk-상처는-스키마-버전을-올리지-않고-열-때마다-자가-복구한다) | 09-16 | 마이그레이션이 남긴 `pending` FK 상처는 열 때마다 자가 복구 | T39 |
| [D-39](#d-39--2026-09-17--보존-정리는-하루에-한-번-지우는-것은-끝난-이력-뿐--pending-30일--task-90일--events-부서당-50000건) | 09-17 | 보존 정리는 하루 한 번, 끝난 이력만 | T30 |
| [D-40](#d-40--2026-09-17--데몬은-하나만--daemonjson-의-pid-가-살아-있고-그-ws-포트가-듣고-있으면-기동-거부exit-3) | 09-17 | 데몬은 하나만 — pid + ws 포트 둘 다 살아 있으면 기동 거부 | T30 |
| [D-41](#d-41--2026-09-17--이벤트-코얼레스는-하지-않는다--실측-부서당-313건분-피크-1초에-6건) | 09-17 | **이벤트 코얼레스는 하지 않는다**(실측 31.3건/분, 피크 6건/초) | T31 |
| [D-42](#d-42--2026-09-17--레이아웃-v2--시선-서열pending-주인-하나범례-7칸규모-규칙) | 09-17 | **레이아웃 v2** — 시선 서열·pending 주인 하나·범례 7칸·규모 규칙 | T32 |
| [D-43](#d-43--2026-09-17--렌더러는-custompainter-유지-스프라이트는-3232-정수-배율-에셋은-cc0서체는-ofl) | 09-17 | 렌더러 CustomPainter 유지, 스프라이트 32×32 정수 배율, CC0 에셋·OFL 서체 | T32·T33 |
| [D-44](#d-44--2026-09-17--스프라이트는-cc0-팩이-아니라-저장소-안의-결정적-픽셀-코드로-생성한다-d-43-4-수정) | 09-17 | 스프라이트는 CC0 팩 대신 **저장소의 결정적 픽셀 코드**로 생성(D-43 §4 수정), 범례 아이콘은 아틀라스 | T33 |

---

## D-01 · 2026-09-14 · 연동 방식: 실제 CLI TUI를 pty로 띄운다
- 맥락: rev 1 설계는 Agent SDK / codex app-server 프로그램 연동.
- 결정: `claude` / `codex` 인터랙티브 CLI를 node-pty에 그대로 띄우고 타이핑으로 지시한다. 상태는 hooks로.
- 버린 대안: Agent SDK(별도 크레딧·터미널 없음), app-server, hooks만(지시 불가).
- 근거: 사용자 의도("그냥 cli를 켜서 거기다 명령"), 구독 그대로 사용, 터미널 화면 공짜. 실측 02 전 항목 통과.

## D-02 · 2026-09-14 · 데몬은 Flutter와 분리된 상주 프로세스
- 맥락: 자식 프로세스(A) vs 상주 데몬(C).
- 결정: localhost WebSocket 데몬. 앱을 꺼도 세션 유지, 나중에 모바일 클라이언트.
- 근거: 사용자 "밖에서 모바일앱으로 작업 진행시키고 싶다".

## D-03 · 2026-09-14 · hook 명령은 Node 스크립트 하나로, 멤버 식별은 환경변수
- 맥락: curl.exe는 Claude(bash)에선 되고 Codex에선 exit 1.
- 결정: `node <슬래시경로>/hook.js <event>` 공통. 스폰 시 `PIXEL_MEMBER` env 주입 → hook이 상속 → URL `/hook/<token>/<event>`.
- 버린 대안: 세션별 settings에 토큰을 박는 방식(Codex는 cwd 단위 hooks.json이라 팀원 간 공유돼 불가).
- 근거: 02 ④ 프로브 결과.

## D-04 · 2026-09-14 · 허가·질문은 hook 결정 반환으로, 키 입력은 다이얼로그 통과 전용
- 결정: `PermissionRequest` hook 응답(`decision.behavior`, `updatedInput.answers`)으로 TUI 프롬프트를 대체. 키 입력(↓, Enter)은 온보딩·폴더 신뢰 다이얼로그에만.
- 근거: 02 ②③④ 실측. TUI 키 매핑 의존을 최소화.

## D-05 · 2026-09-14 · 멤버 지시문은 SessionStart hook additionalContext로 주입
- 결정: 프로젝트 CLAUDE.md/AGENTS.md는 건드리지 않는다. startup/clear/resume/compact마다 재주입.
- 버린 대안: `--append-system-prompt`(확인 불필요해짐), 첫 메시지 타이핑(압축 시 유실).

## D-06 · 2026-09-14 · 팀 구조: 팀장/팀원 2단, 팀장 = 오케스트레이터
- 결정: 사용자→팀장만 지시. 팀장이 `hire/dismiss/delegate/report`. 사용자는 출근/퇴근으로 개입, 사용자가 출근시킨 팀원은 팀장이 못 자름.
- 근거: 사용자 요구(2026-09-14).

## D-07 · 2026-09-14 · 데몬 재시작 = 전원 resume
- 맥락: ConPTY 자식이 부모와 함께 죽는 것 확인(02 ①).
- 결정: 유령 프로세스 정리 없음. 기동 시 `claude --resume` / `codex resume`로 재스폰 + `[RESUMED]`. approval pending은 expired, question pending 유지.

## D-08 · 2026-09-14 · 데몬 기술 스택
- 결정: TypeScript + Node 24 + `tsx`, DB `node:sqlite`(내장), `ws`, `node-pty`, `@xterm/headless`.
- 버린 대안: better-sqlite3(네이티브 빌드), Dart 데몬(pty 바인딩 불확실).

## D-09 · 2026-09-14 · M1 사무실 렌더링은 CustomPainter, Flame은 M6에서 재검토
- 근거: 캐릭터 10명 이하·단순 이동. 스프라이트 애니메이션 요구가 커지면 M6에서 Flame 전환.

## D-10 · 2026-09-14 · 문서화 규칙
- 결정: 태스크당 worklog 1개, 결정은 이 파일에 append, 설계문서는 수정 대신 변경 이력, 코드 폴더 README, 완료 정의 5요소(코드·검증·worklog·작업계획 체크·커밋).

## D-11 · 2026-09-14 · hook 보류 타임아웃은 deny가 아니라 pass-through(`{}`)
- 맥락: HookReceiver(T03)가 결정을 24시간까지 보류. 상한을 넘기면 어떻게 답할지.
- 결정: `{}`(결정 없음)를 돌려주고 `hold-timeout` 이벤트. deny로 명령을 포기시키는 것보다 CLI 기본 흐름(TUI 프롬프트)으로 넘기는 게 안전.
- 근거: 설계문서 "보류 중 hook 타임아웃" 항목. 어댑터(T04)가 필요하면 더 짧은 자체 타이머로 deny를 얹을 수 있음.

## D-12 · 2026-09-14 · node-pty ConPTY 자연 종료 시 내부 리소스를 직접 해제
- 맥락: T01 통합 테스트에서 `/exit`로 자식이 끝나도 node-pty 1.1.0(win32)이 conout 워커 스레드·pseudoconsole 핸들을 풀지 않아 데몬 프로세스가 종료되지 않음. `proc.kill()`로 우회하면 `conpty_console_list_agent`가 포크되어 stderr 스택과 5초 대기.
- 결정: `onExit`에서 `releaseConptyResources(proc)`로 내부 필드 3개를 직접 해제, 필드 형태가 바뀌면 `proc.kill()` 폴백. node-pty 업그레이드 시 재확인 항목.
- 근거: T01 기록.

## D-13 · 2026-09-14 · TUI 패턴 JSON에 `verified` 플래그
- 결정: `src/tui-maps/*.json`의 각 패턴에 실측 로그 출처와 `verified` 여부를 적는다. 미검증(Claude "Login successful", Codex busy/interrupted)은 M3/M0 시연에서 캡처해 채운다.
- 근거: T02 기록.

## D-14 · 2026-09-14 · SessionEnd reason `clear`·`resume`은 프로세스 종료가 아니다
- 맥락: 터미널 탭에서 `/clear`·`/resume`을 치면 같은 프로세스 안에서 `SessionEnd`→`SessionStart`가 온다.
- 결정: 어댑터(T04)는 이 두 reason을 비종료로 취급하고 상태를 바꾸지 않는다. 실제 종료는 pty `exit`(→ `onSessionExit`)로만 판정.

## D-15 · 2026-09-14 · hook 보류가 결정 없이 끝나면 `error` 이벤트 + "재지시 필요"
- 맥락: D-11의 pass-through 후 사용자에게 무슨 일이 있었는지 보여야 함.
- 결정: `hold-timeout`/`hold-closed` 시 pending을 expired로 바꾸고 `error{summary, pendingId}` 이벤트를 남긴다. 상태는 `working`(CLI가 자기 TUI 프롬프트를 쥔 상태).

## D-16 · 2026-09-15 · 세션 hooks `timeout`은 86400초, 만료 시 TUI 프롬프트가 폴백
- 맥락: T07 콘솔 검증 중 허가를 10분 넘게 방치하자 Claude가 hook(`timeout: 600`)을 끊음 → `hook connection closed before answer` error 이벤트 → pending 만료 → CLI 자체 TUI 허가 프롬프트 표시(D-11 pass-through 동작 확인). `type 하루 1`로 TUI에서 승인해 정상 완료.
- 결정: hook `timeout`을 설정값(`config.hookTimeoutSec`, 기본 86400)으로 올린다. 그래도 만료되면 "재지시 필요" 카드 + 터미널 탭에서 직접 답하는 것이 공식 폴백. 86400이 실제로 받아들여지는지는 T10에서 10분 이상 보류로 확인.

## D-17 · 2026-09-15 · 데몬 강제 종료 시 살아남는 CLI 자식은 재시작 때 정리(reap)한다 — D-07 일부 정정
- 맥락: 스파이크(`orphan-test.js`)는 부모가 `process.exit`로 **정상** 종료할 때만 자식이 죽는 걸 봤다. T09 통합 테스트에서 데몬을 `process.kill`로 **강제** 종료하니 `claude.exe`가 살아남아 새 데몬에 같은 멤버 토큰으로 hook을 계속 보냈다.
- 결정: `recover()`가 `--resume` 전에 `child_pid`가 살아 있고 `tasklist` 이미지명에 엔진 이름이 들어 있으면 kill(PID 재사용 방지 가드), 아니면 경고만. `child_pid` 기록은 이 용도로 필수.
- 근거: T09 기록.

## D-18 · 2026-09-15 · resume 실패 폴백은 1회, `session_id`를 비운다
- 결정: `--resume`한 프로세스가 10초 안에 비정상 종료하면 `session_id`를 지우고 한 번만 새 세션으로 재스폰(`error{summary:'resume failed; started fresh session'}`). 두 번째 죽음은 일반 오류 경로.

## D-19 · 2026-09-15 · 질문 pending의 TUI/ask_user 구분은 `payload.tool_input` 유무
- 결정: `tool_input`이 있으면 TUI `AskUserQuestion` 메뉴(프로세스와 함께 사라짐 → 재시작 시 만료), 없으면 MCP `ask_user`(턴 종료 상태 → 유지).

## D-20 · 2026-09-15 · `[RESUMED]` 요약은 쓰기 전에 스냅샷
- 결정: 복구 중 pending 만료·error 이벤트를 쓰기 **전에** 배정 task·최근 5개 이벤트를 읽어 요약한다. 그래야 "죽기 직전" 그림이 모델에게 간다.

## D-21 · 2026-09-15 · 재접속 시 이벤트 링은 `events.query`로 백필
- 맥락: PROTOCOL 규칙 2(`seq > snapshot.seq`만 적용)를 그대로 따르면 끊긴 동안의 replay 이벤트가 로그·말풍선에서 사라진다(T08·T11 동일 관찰). 상태는 스냅샷이 맡으므로 정합성 문제는 없음.
- 결정: 클라이언트는 스냅샷 적용 후 `events.query{beforeSeq: snapshot.seq+1, limit}`로 최근 이벤트를 받아 링 버퍼를 채운다(표시 전용). replay 알림은 계속 무시.

## D-22 · 2026-09-16 · TeamTools MCP 도구 호출(`mcp__team__*`)은 데몬이 자동 허가
- 맥락: T17 통합 테스트에서 Claude 2.1.270이 첫 `ask_user` 호출에 `PermissionRequest`를 띄움 → 앱에 허가 카드가 먼저 뜨고 그 다음 질문 카드가 뜨는 어색한 흐름.
- 결정: 어댑터에서 `tool_name`이 `mcp__team__`로 시작하면 사용자에게 올리지 않고 allow(우리 도구는 부작용이 데몬 안에서만 생김). T19에서 구현·확인.
- 참고: 2.1.270은 MCP 도구를 지연 로딩(`ToolSearch` 후 노출)하므로 팀장 프롬프트에 도구 이름을 명시해 두는 게 좋다(M4).

## D-23 · 2026-09-16 · Codex 종료는 Ctrl+C 1회 후 확인, TUI 승인 프롬프트는 자동 통과 대상이 아님
- 맥락(T21 실측): Codex는 유휴 프롬프트에서 Ctrl+C **한 번**에 종료(`codex resume <id>` 안내 출력). Claude의 "Press Ctrl-C again to exit" 창은 1초 이내. Codex 부팅 화면(`model: loading`, ~4초)은 프롬프트가 보여도 Enter가 씹힘.
- 결정: ① Codex 정중 종료 = Ctrl+C 1회 → 2초 대기 → 살아 있으면 1회 더 → 타임아웃 시 강제 종료. ② `promptReady`에 `noneOf`(예: `model: loading`) 추가. ③ 승인 프롬프트(`approval-prompt`)는 `detectDialog`가 `suggestedKeys: []`로 돌려주고 InputQueue가 자동으로 키를 보내지 않는다 — "재지시 필요" 폴백에서만 `approvalPrompt().allowKeys/denyKeys`를 쓴다.
- 참고: Codex 쪽 **모델 턴이 필요한 실기 검증**(T20 통합·T22·T23)은 계정 사용량 한도로 이월했고, 코드·단위 테스트를 먼저 닫았다. Codex 승인 프롬프트 문구는 바이너리에서 추출한 것(`verified:false`)이라 `test/screen/tools/capture-codex.ts` 로 재캡처해야 확정된다.

## D-24 · 2026-09-16 · Codex 부팅 완료는 화면(prompt-ready)으로 판정, SessionStart는 첫 프롬프트 때 온다
- 맥락(T20): Codex hooks의 `SessionStart`는 부팅 시가 아니라 **첫 프롬프트 제출 시** 발화. hook만 기다리면 멤버가 영원히 `starting`이라 첫 지시가 안 들어감.
- 결정: Codex 멤버는 `Office.watchBootReady()`가 ScreenModel prompt-ready(다이얼로그 없음, `model: loading` 아님)를 보고 `starting → idle`로 올린다. SessionStart가 먼저 오면 그걸로.
- 참고: node-pty는 PATH의 `codex` 셸 shim(.cmd/.ps1)을 못 띄움 → `config.codexExe`는 실제 `codex.exe` 경로여야 함(T22에서 npm 전역 경로 자동 탐색).

## D-25 · 2026-09-16 · 턴 종료 hook 이 없으면 화면으로 idle 을 판정한다(`idle{summary:'screen-idle'}`)
- 맥락(T23 함정 1): Codex 사용량 한도 안내는 화면에만 찍히고 `Stop` hook 이 오지 않는다. 데몬은 `UserPromptSubmit` 으로 올려 둔 `working` 을 내려 줄 근거가 없어 멤버가 퇴근할 때까지 `working` 에 갇힌다 — ① 사무실 표시가 틀리고 ② `InputQueue.isIdle` 게이트가 안 열려 다음 지시가 큐에 머문다. 원인은 한도가 아니라 "턴 종료 hook 없이 프롬프트로 돌아오는 화면" 일반이고, Claude 도 턴이 이상하게 끊기면 같은 일이 난다.
- 결정: `Office.watchScreenIdle()` — 엔진 공통, 멤버당 타이머 하나, 500ms 폴링(`watchBootReady`/`watchInterrupted` 와 같은 모양). status 가 `working`/`waiting_approval`/`waiting_answer` 이고, **열린 pending 이 하나도 없고**, 화면에 다이얼로그·busy 표시가 없고 prompt ready 이고, 그 사이 새 hook 이 도착하지 않은 상태가 **3초 연속**이면 `idle{summary:'screen-idle'}` + status `idle`. 조건이 하나라도 깨지면 창을 닫고 처음부터 다시 잰다. 세션이 끝나면 타이머를 멈춘다.
- 버린 대안: ① 엔진별(Codex 전용) 폴백 — 원인이 엔진이 아니라 화면이다. ② status 만 바꾸고 이벤트는 안 남기기 — 앱 로그에서 "왜 내려왔나"를 설명할 수 없다. ③ 창을 더 짧게(1초) — 화면이 잠깐 준비로 보이는 구간(도구 출력 사이)에서 오발화 위험.
- 근거: T23b 기록, `test/office/ScreenWatch.test.ts`(실측 픽스처 `codex/usage-limit.txt` · Claude 준비 화면). 보류·busy·다이얼로그 중에는 발화하지 않는 것을 테스트로 고정.

## D-26 · 2026-09-16 · `approval-prompt` 은 "통과" 가 아니라 "막힘" 이다 — `dialogPassed` 대신 `blocked`+`dialogBlocked`
- 맥락(T23 함정 2, T21 후속): Claude 2.1.270 은 `PermissionRequest` hook 을 보류하는 동안에도 자기 TUI 허가 프롬프트를 띄운다. ScreenModel 은 이걸 `approval-prompt` 로 감지하고 D-23 대로 `suggestedKeys: []` 를 준다(키 안 나감 = 안전). 그런데 InputQueue 가 빈 키 시퀀스를 "보냈다" 치고 `dialogPassed` 를 emit 해 `daemon.notice` 에 `passed first-run dialog (approval-prompt)` 가 허가 대기 내내 2초마다 찍혔다(실기 7회).
- 결정: InputQueue 는 `suggestedKeys` 가 비어 있으면 키를 보내지 않고 `dialogPassed` 도 내지 않는다. 대신 ① `blocked('dialog')`(다른 blocked 이유와 같은 5초 계량, 큐에 기다리는 항목이 있을 때만) ② 새 이벤트 `dialogBlocked(kind)` — **그 다이얼로그가 사라질 때까지 kind 당 한 번**. Office 는 `dialogBlocked('approval-prompt')` 를 `daemon.notice{level:'warn', message:'<이름>: CLI 허가 프롬프트가 떠 있음 — 카드로 답하거나 터미널에서 직접 답하세요'}` 한 번으로 옮긴다(사라졌다 다시 뜨면 다시 한 번).
- 버린 대안: 알림을 아예 없애기 — 사용자는 "왜 지시가 안 들어가지?" 를 알아야 한다. 허가 키(`approvalPrompt().allowKeys`)를 데몬이 대신 누르기 — D-23 을 뒤집는 것이라 안 한다.
- 근거: T23b 기록, `test/input/InputQueue.test.ts`(빈 키 시퀀스 3건), `test/office/ScreenWatch.test.ts`(실측 픽스처 `claude/approval-prompt.txt` 로 알림 1회 + 키 0개).

## D-27 · 2026-09-16 · 팀 셸 뮤텍스는 `PreToolUse` 에서 잡되 **읽기 전용 명령은 잡지 않는다**
- 맥락(T27): 설계(01 §"팀 단위 셸 뮤텍스")는 "허가 여부와 무관하게 `PreToolUse` 에서 잡는다" 였다 — 실측 02 §② 대로 읽기 명령은 `PermissionRequest` 없이 실행되므로 허가를 기준으로 잡으면 락이 새기 때문이다. 그런데 그대로 하면 `ls`·`cat`·`git status` 까지 팀 전체가 한 줄로 서게 된다(팀원 셋이 같은 파일을 읽기만 해도 직렬화). 읽기는 서로 부딪히지 않는, 뮤텍스가 막으려던 대상(동시 빌드·테스트·쓰기)이 아니다.
- 결정: 락 대상 = **셸 도구(`Bash`/`PowerShell` + Codex 셸 도구) ∧ 읽기 전용이 아님**. 판정은 두 엔진 모두 `src/adapters/codexMapping.ts` 의 `isReadOnlyCommand` 하나로 한다(Codex 이벤트 매핑이 이미 쓰는 함수 — 애매하면 "쓰기"로 보는 보수적 함수라 락을 놓치지 않는다). 명령 문자열을 못 읽으면 잡는다.
- 딸린 결정: ① 해제는 `tool_use_id` 로 짝을 맞춘다(`PostToolUse` / `PostToolUseFailure`, 실측 02 §②). ② 보유 상한 30분 초과 시 강제 해제 + `daemon.notice{warn}`. ③ **보류가 먼저 끊긴 대기자는 줄에서 뺀다** — D-11 대로 pass-through 가 나간 명령은 이미 실행되므로, 줄에 남겨 두면 아무도 안 기다리는 락을 넘겨받아 팀이 굳는다(어댑터가 `toolGate` 에 `AbortSignal` 을 준다). ④ 락을 **이미 쥔** 쪽은 hook 이 끊겨도 풀지 않는다(명령은 계속 돌고 있다) — 해제는 `toolDone`·후처리·상한뿐. ⑤ 대기 표시는 `running{waiting:'shell-lock'}` 한 번이고 status 는 `working` 그대로. 그래서 `PreToolUse` 의 도구 이벤트를 게이트보다 **먼저** 남긴다(마지막 이벤트 = 말풍선 = "대기 중").
- 버린 대안: 허가(`PermissionRequest`) 기준으로 잡기 — 읽기 명령이 락 없이 지나가는 게 아니라 **쓰기 명령도** 자동 허가(`alwaysThisSession`) 되면 새기 때문에 안 된다. 모든 셸 명령을 잡기 — 위 맥락. 락을 못 잡으면 deny — D-11 을 뒤집는다.
- 근거: T27 기록, `src/office/ShellMutex.ts`, `test/office/ShellMutex.test.ts`.

## D-28 · 2026-09-16 · "살아 있는 팀장" 판정과 team.create 반환 변경
- 결정(T24): `team.create`는 팀장을 자동 출근시키고 `{team, leader}`를 돌려준다(이전 `{team}`에서 breaking). "살아 있는 팀장" = `rank:'leader' ∧ status ∉ {exited, error}`; `leaderId`는 팀장이 나가도 남는다. 팀원 직접 지시는 살아 있는 팀장이 있으면 -32004, `force:true`는 디버그 탈출구.
- 근거: T24 기록.

## D-29 · 2026-09-16 · 보고를 기다리는 턴 종료는 "끝난 작업"이 아니다 + 위임 task 의 승격도 팀장 버퍼를 탄다
- 맥락(T25 실기 1회차): 팀장이 `hire`+`delegate` 를 하고 "맡겼고 보고를 기다리는 중입니다" 라고 말하며 턴을 끝내자, v1a 보고 승격(`Stop` → 그 멤버의 `assigned` task 를 `reported`, report_text = 마지막 메시지)이 **사용자 task 를 그 인사말로 닫아 버렸다.** 나중에 팀장이 진짜 `report(task#1)` 을 부르자 데몬이 `"task#1 은(는) 이미 보고됐습니다"` 로 거절했고, 모델은 사용자에게 "사무실 쪽에서 먼저 닫힌 것 같다" 고 사과했다. 반대 방향의 구멍도 있었다 — 팀원이 위임 task 를 `report` 없이 말로만 끝내면 task 는 `reported` 가 되지만 팀장에게 `[REPORTS]` 가 가지 않아 `[ALL_REPORTS_IN]` 을 영영 못 받는다.
- 결정: ① **그 멤버가 발행한(delegate) 미종료 task 가 하나라도 있으면 턴 종료 승격을 건너뛴다** — 보고를 기다리려고 턴을 끝낸 것이지 일이 끝난 게 아니다. 그 task 는 `[ALL_REPORTS_IN]` 뒤 턴의 승격이나 `report` 도구가 닫는다. ② 승격으로 닫히는 task 의 발행자가 팀장이면 **`report(status:'done')` 과 같은 버퍼를 태운다** — 팀원이 도구를 잊어도 팀장이 굳지 않는다.
- 버린 대안: 승격을 아예 없애고 `report` 만 인정 — 모델이 도구를 안 부르는 턴이 실재하고(T22 Codex 폴백, D-25) v1a 보고가 통째로 사라진다. 팀장만 예외로 두기 — 조건은 직급이 아니라 "발행한 미종료 task 가 있는가" 다(팀원이 위임받을 수 있게 되면 그대로 맞는다).
- 근거: T25 기록(실기 로그 1·2회차), `test/office/TeamTools.test.ts`("보고를 기다리는 팀장의 턴 종료는 사용자 task 를 닫지 않는다", "report 없이 턴만 끝내도 팀장 버퍼를 탄다").

## D-30 · 2026-09-16 · 주입되는 것은 "파일" 이 아니라 **프리앰블 + 유효 지시문** — 기본 템플릿은 저장하지 않고 매번 계산한다
- 맥락(T26b): 설계 01 §멤버 지시문 주입은 "사용자가 앱에서 편집, 비어 있으면 데몬 기본 템플릿" 까지만 정했다. 그대로 만들면 두 구멍이 남는다. ① 사용자가 지시문을 쓰는 순간(예 "말투는 반말") 모델은 자기 이름·직급·팀·팀원 목록·도구 이름을 **전부 잃는다** — 사용자가 쓴 글에는 그런 게 없다. ② `hire` 는 `# 역할: <role>` 한 줄을 파일로 쓰는데(T25), 그 파일이 "비어 있지 않다" 는 이유로 기본 템플릿을 덮으면 팀원은 `report` 를 써야 한다는 것조차 모른다(T25 "남은 것" 에 적힌 문제 그대로).
- 결정: `SessionStart` 의 `additionalContext` = **런타임 프리앰블 + 유효 지시문**.
  ① **프리앰블은 사용자 파일이 있어도 늘 붙는다** — 정체(이름·직급·팀·엔진·작업 폴더) + 살아 있는 팀원 로스터(`팀원: 이음(claude, 역할 …)`) + 직급별 도구 이름 줄. startup/resume/clear/compact 마다 다시 오므로(D-05) 리셋돼도 모델이 자기와 팀을 잃지 않는다.
  ② **유효 지시문** = 사용자 파일에 본문이 있으면 그 파일, 없으면 직급별 기본 템플릿. **`# 역할: <role>` 한 줄뿐인 파일은 "본문 없음"** 으로 본다 — 그건 사용자가 쓴 지시문이 아니라 `roleOf` 가 되읽는 역할 메타데이터라, 기본 템플릿을 덮는 대신 템플릿 머리말 `- 역할: …` 에 실린다.
  ③ **기본 템플릿은 파일로 저장하지 않는다.** 파일은 사용자(앱·`instr set`)나 `hire`/`clockIn` 의 `instructions` 로만 생긴다. 기본값은 주입할 때마다 계산하므로 템플릿 문장을 고치면 지시문을 따로 쓰지 않은 멤버 전원에게 다음 SessionStart 부터 반영된다.
  ④ 전체 3000자(한국어 기준 ~2500토큰) 상한. 넘으면 **사용자 본문만** 자르고 표식을 붙인다 — 프리앰블은 통째로 남는다.
  ⑤ `member.instructions.get` 은 여전히 **사용자 파일만** 돌려준다(편집기가 보는 값). 주입될 텍스트는 새 `member.instructions.effective` 로 본다(콘솔 `instr effective`).
- 버린 대안: ① 출근할 때 기본 템플릿을 파일로 써 두기 — 템플릿을 고쳐도 기존 멤버에게 반영되지 않고, 사용자가 "내가 안 쓴 글" 을 편집기에서 보게 된다. ② 프리앰블을 기본 템플릿 안에만 넣기 — 사용자가 지시문을 쓰는 순간 사라진다(위 구멍 ①). ③ 프리앰블을 `[ROLE]` 시스템 메시지로 타이핑하기 — compaction 에 유실된다(설계가 `additionalContext` 를 1순위로 고른 이유).
- 근거: T26b 기록(실기 로그 — 팀장이 "저는 픽셀 오피스 팀 it26b의 팀장 반장(claude)이고, 팀원은 파일 작성을 맡은 이음(claude) 한 명이에요" 라고 답한다), `src/office/instructions/`, `test/office/Instructions.test.ts`.

## D-31 · 2026-09-16 · 후처리는 표 하나, 파생 상태는 함수 하나 — `member.restart` 만 `ask_user` 예외를 못 받는다
- 맥락(T28): "멤버가 하던 일을 거두는" 코드가 `interrupt`/`clockOut`/`restart`/`onPtyExit`/`recover`/`deleteTeam` 여섯 군데에 흩어져 조각마다 빠진 게 달랐다 — **팀 삭제는 셸 락도 MCP 토큰도 정리하지 않았고**, 팀장을 먼저 내보내는 바람에 이미 나가는 팀장에게 aborted 보고가 되돌아왔다. 파생 상태도 데몬(`Office.derived`)과 앱(`DerivedStatus.fromStatus`)이 따로 계산해 재접속 직후 값이 어긋났다.
- 결정: ① 후처리는 `src/office/afterCare.ts` 의 `settleMember(store, ctx, memberId, reason)` 하나 + `SETTLE_MATRIX` 표(이유 6개 × 정책 6칸, PROTOCOL.md "후처리" 절과 같은 표). 여섯 경로는 전부 이 문으로 들어간다. ② 파생 상태는 `src/office/derived.ts` 의 `derivedStatus()` 하나이고 **snapshot 과 `member.status` 알림이 같은 함수를 쓴다** — `snapshot.members[]` 에 `derived` 를 실어 보내 클라이언트가 규칙을 두 번 구현하지 않게 한다(앱은 값이 없을 때만 예전처럼 유추). ③ raw status 가 그대로여도 **파생이 바뀌면** `member.status` 를 한 번 더 보낸다(T17 의 `ask_user` 예외를 일반화). ④ `team.delete` 는 **팀원 먼저, 팀장 마지막**. ⑤ 파생 판정에서 허가·질문은 raw 와 무관하게 앞서고, `exited`/`error` 는 파생이 덮지 않는다.
- 딸린 결정(한계) → **D-36 으로 대체**: **`member.restart` 는 `ask_user` 질문을 살리지 않는다.** D-19 대로라면 턴 종료 상태의 질문은 프로세스가 죽어도 유효해야 하지만, 살아 있는 프로세스를 죽이는 경로에서는 hook 보류 취소와 pending 만료가 어댑터의 `expireAllForMember` 하나로 묶여 있다(보류를 안 끊으면 CLI 가 응답을 기다리며 멈춘다). 어댑터에 "보류만 취소" 를 따로 내는 것은 T28 범위(`src/adapters/**` 불가) 밖이라 표의 `restart` 행을 "전부 만료" 로 적고 T30 후보로 남긴다. 데몬 재시작 복구(`recover`)는 in-memory 보류가 아예 없으므로 D-19 그대로 `ask_user` 질문을 남긴다.
- 버린 대안: ① 이유별 플래그를 호출부에서 넘기기 — 지금까지 그랬고, 새 경로를 추가할 때마다 한 칸씩 빠뜨렸다. ② 파생을 클라이언트가 계산하기 — 앱은 열린 task 의 발행자를 모두 알지 못해 `waiting_reports` 를 재접속 직후 틀리게 그렸다. ③ 파생 변경 알림을 `ask_user` 에만 두기(T17 그대로) — 같은 문제가 `delegate`·후처리에서 반복된다.
- 결과: `free` 는 팀장에게도 그대로 쓴다(T25 테스트가 그렇게 고정해 뒀고 의미도 맞다). 앱 모니터 문구는 파생 `free` 일 때 "(대기)" 대신 "(한가함)".
- 근거: T28 기록, `src/office/afterCare.ts`, `src/office/derived.ts`, `test/office/AfterCare.test.ts`(14건), PROTOCOL.md "멤버 파생 상태 `derived`" · "후처리".

## D-32 · 2026-09-16 · 직무 체계 rev 3: 나 → 부장 → 팀장 → 팀원 (트리)
- 맥락: T29 시연 준비 중 사용자 관찰 — "팀장 AI가 내가 추가한 팀원을 못 찾는다". 고용 주체가 둘(사용자·팀장)이라 팀장의 인식과 실제 구성이 어긋남. 사용자 제안으로 체계를 다시 잡음.
- 결정:
  1. **직급 3단 트리**: 사용자는 **부서**를 만들고 **부장**만 임명한다. 부장은 팀을 만들고 팀장을 배치한다. 팀장은 팀원을 만든다. 고용·해고·지시는 항상 **바로 아래**로만, 보고·질문은 항상 **바로 위**로만. 사용자가 팀장·팀원을 직접 출근/퇴근시키는 기능은 없앤다(디버그 `force`만).
  2. **부서 = 프로젝트(cwd)**. 한 부서 안의 팀들은 같은 cwd를 쓰고 팀장이 디렉토리를 나눠 맡긴다. 팀별 repo/worktree 분리는 v2.
  3. **셸 허가(Bash 승인)는 계속 사용자에게.** 보고 체계와 무관한 안전 문제. 카드가 늘어나므로 "이번 세션 항상 허가"를 팀 단위로 넓히는 옵션을 검토(T34).
  4. **사용자에게 직접 오는 것은 부장뿐**: `ask_user`와 사용자 보고는 부장 전용. 팀장·팀원은 `ask_parent`/`report`로 바로 위에만 올리고, 부장이 판단해 올리는 것이 "진짜 중요한 문제"다.
  5. 비용 고려: 팀장은 팀원 0명으로 직접 일해도 된다(작은 지시에 세션 3개를 강제하지 않음). 부장이 팀 없이 팀원을 두는 것은 금지.
- 영향: M4(T24~T29)의 2단 구현은 **M4b**로 재편해 트리로 일반화한다. hire/delegate/report·`[REPORTS]` 버퍼링·셸 뮤텍스·후처리 매트릭스·지시문 템플릿은 그대로 재사용. 데이터 모델은 `departments` + `members.parent_id` + `rank ∈ {head, lead, member}`.
- 사용자 원문: "나 - 부장 - 팀장 - 팀원 이렇게 내려가도록하자 … 진짜 중요한 문제가 있어서 내게 직접 보고하는게 아니면 보고는 각 상위 직급에게".

## D-33 · 2026-09-16 · 트리 스키마의 소유 관계: task·event·셸 락은 **부서** 소유, 부장은 팀에 속하지 않는다
- 맥락: T34(D-32 구현)에서 스키마를 v2로 올리며 "팀" 을 가정하던 세 곳이 걸렸다 — ① 부장은 어느 팀에도 속하지 않는데 `members.team_id` 가 NOT NULL, ② 사용자 task 는 팀 없는 부장이 받는데 `tasks.team_id` 가 팀 FK, ③ 셸 락이 팀 단위인데 D-32에서 **한 부서의 팀들이 같은 cwd** 를 쓰기로 했다.
- 결정:
  1. `members.team_id` 를 **nullable** 로. 부장은 `team_id null`(부서 직속), 팀장·팀원만 팀에 속한다. 클라이언트도 `Member.teamId: string | null`.
  2. `tasks.team_id` → **`department_id`**(부서 FK, cascade). 부작용으로 `team.delete` 가 더는 보고 이력을 지우지 않는다 — T29 결함 ⑤("`team delete` 가 tasks 행을 지움")가 여기서 해소됐다(T36 몫이었지만 스키마가 먼저 정리됨).
  3. `events` 는 `department_id` 를 **추가**하고 `team_id` 는 남긴다(팀 없는 부장은 `''`). 이력 테이블이라 재작성 비용이 크고, 팀 단위 조회도 아직 쓸 데가 있다.
  4. **셸 락 범위 = 부서**(`ShellMutex` 키를 `teams.id` → `departments.id`). 같은 cwd 에서 두 팀이 동시에 빌드하면 막아야 한다.
  5. `members.hired_by` 값은 `'user' | 'leader'` 그대로 둔다. 의미만 "사용자 / 상위 멤버" 로 읽는다 — 행 마이그레이션과 앱 문자열을 아끼는 쪽.
- 버린 대안: 부장용 더미 팀을 만들어 `team_id` NOT NULL 유지(트리를 거짓말하게 만든다), `events` 통째 재작성(414행 개발 DB에 비해 과함), `hired_by` 값 `'parent'` 로 변경(이득 없는 churn).
- 결과: v1 DB 한 개(개발 머신)는 `Store.migrate()` 가 열 때 한 번에 옮긴다 — 팀 하나 = 부서 하나, 그 팀장이 부장이 되고 옛 팀은 그 부서의 팀으로 남는다(부장이 레거시 팀의 팀장을 겸한다). 실측: 기동 시 v1(events 414행) → v2 정상 이전, `test/store/Migration.test.ts` 4건.

## D-34 · 2026-09-16 · 없어진 사용자 기능은 지우지 않고 `force:true` 뒤로 숨긴다
- 맥락: D-32로 "사용자가 팀·팀원을 직접 만드는 기능" 이 사라졌다. 그런데 콘솔·테스트·망가진 트리 복구에는 그 길이 여전히 필요하다.
- 결정: `team.create` 와 `member.clockIn` 은 RPC 로 남기되 **`force:true` 가 없으면 -32004**. 앱은 절대 보내지 않고, 콘솔(`team create`, `hire`)만 붙여 보낸다. `member.clockOut` 은 **누구에게나 그대로 허용** — 굳은 세션을 사용자가 치울 비상구가 없으면 안 된다(문서에 "비상구" 로 명시).
- 결정(부수): `member.clockIn` 에 `parentId` 를 주면 정식 고용 경로와 **같은 직급 사슬 검사**를 받고, 주지 않으면 부서·팀에 바로 꽂는다(부모 자동 결정, 없으면 부모 없는 행). 사슬 강제는 `Office.hireChild` 한 곳에만 둔다.
- 버린 대안: 메서드 삭제(콘솔·테스트가 통째로 막힌다), 별도 `debug.*` 네임스페이스(메서드 표가 두 벌이 된다).
- 결과: 앱(T37)은 `department.create` + 퇴근/중단/재시작만 쓰면 된다. 콘솔은 `dept create` 가 기본 길이고 `team create`/`hire` 는 디버그로 표시.

## D-37 · 2026-09-16 · `ask_parent` 는 `ask_user` 와 **같은 pending 한 종류**로 두고, 답하는 길을 둘로 연다
- 맥락: T35에서 팀장·팀원이 위에 묻는 `ask_parent` 를 만들면서 두 가지를 정해야 했다 — ① 새 pending 타입을 만들 것인가, ② 상사가 굳었을 때 그 질문을 누가 끊어 주는가.
- 결정:
  1. **pending 타입은 `question` 하나 그대로.** payload 만 `{source:'ask_parent', question, options, from, to}` 로 다르다(`tool_input` 없음 — D-19 규칙을 그대로 받아 재시작 복구에서 열린 채 남는다). 파생 상태(`waiting_answer`)·스냅샷·재시작 복구·후처리 만료가 전부 손대지 않고 그대로 동작한다.
  2. **답하는 길은 둘.** 정식은 상사의 `reply({to_member, text})` — 그 부하에게 **나를 향해 열린** `ask_parent` 가 있으면 답으로 닫고 `[ANSWER q#n]`, 없으면 `[MESSAGE from <나>]`. 도구를 둘로 쪼개지 않은 이유는 모델이 "지금 열린 질문이 있는지" 를 기억할 필요가 없어야 하기 때문이다(데몬이 안다). 보조는 사용자의 `question.respond` — 앱 UI 에 **광고하지 않는 오버라이드**로, 상사 세션이 굳었을 때 사용자가 끊어 줄 비상구다(D-34 의 `member.clockOut` 과 같은 성격).
- 버린 대안: 새 pending 타입 `parent_question`(파생·복구·후처리 표를 전부 두 벌로 만든다), `reply` 와 `answer` 를 별도 도구로(모델이 상태를 추적해야 해서 틀리기 쉽다), `ask_parent` 를 블로킹 MCP 호출로(턴이 붙잡혀 상사가 답할 때까지 CLI 가 멈춘다 — T17에서 이미 버린 길).
- 결과: `[QUESTION from X q#n]` → `reply` → `[ANSWER q#n]` 왕복이 T17 큐 규칙(답이 게이트를 먼저 연다)을 그대로 재사용한다. 실기에서 부장 → 팀장 → 팀원 사슬이 한 번에 돌았다(T35 기록).

## D-35 · 2026-09-16 · `team.delete` 는 보고 이력을 남기고, `department.delete` 는 task 까지 지운다
- 맥락: T29 결함 ⑤("`team delete` 가 tasks 행을 지움" — 팀을 정리하면 그 팀이 받은 지시·보고가 통째로 사라져 사무실 로그가 거짓말을 했다)는 T36 몫이었는데, D-33 이 `tasks.team_id` → `tasks.department_id` 로 옮기면서 스키마 차원에서 이미 해소됐다. 남은 것은 "그럼 부서를 지울 때는?" 을 못 박는 일이다. (번호 35·36 은 T36 용으로 비워 뒀던 자리다 — 같은 날 병행한 T35 가 D-37 을 먼저 썼다.)
- 결정: **팀 삭제는 task 를 지우지 않는다**(팀은 부서 안의 작업 단위일 뿐이고 그 일을 누가 시켰고 무엇을 보고했는지는 부서의 이력이다). **부서 삭제는 task 를 함께 지운다**(`tasks.department_id ON DELETE CASCADE`) — 부서 = 프로젝트(D-32)라 프로젝트 자체가 없어지는 것이고, 남겨 봐야 어느 부서 것인지 가리킬 곳이 없다. `events` 는 둘 다 남는다(이력 테이블, D-33 ③).
- 버린 대안: ① 부서 삭제도 task 를 남기기(고아 행이 쌓이고 `listTasks({departmentId})` 로 다시 찾을 길이 없다). ② task soft delete(`deleted_at`) — 조회하는 곳마다 필터가 늘고, 정작 사용자가 보는 이력은 `events` 로 충분하다.
- 근거: `test/office/TreeAfterCare.test.ts`("결함 ⑤ 회귀: team.delete 는 tasks 행을 지우지 않는다", "department.delete 는 반대로 task 를 함께 지운다"), T36 기록.

## D-36 · 2026-09-16 · hook 보류 취소와 pending 만료를 갈라 `member.restart` 도 턴 종료 질문을 살린다 — D-31 딸린 결정 정정
- 맥락: D-31 은 "`member.restart` 는 `ask_user` 질문을 살리지 않는다" 를 **한계**로 적어 두고 T30 후보로 넘겼다. 이유는 어댑터의 `expireAllForMember` 하나가 ① 열린 hook 보류 취소와 ② pending 행 만료를 묶어 하고 있었고, 보류를 안 끊으면 죽는 CLI 가 응답을 기다리며 멈추기 때문이었다. 그런데 D-19 대로라면 턴 종료 질문(`ask_user`, T35 부터 `ask_parent` 도 — D-37)은 프로세스가 죽어도 유효하다. 사용자가 지시문을 고쳐 재시작했다고 열려 있던 질문이 사라지면 그 질문은 영영 답을 못 받는다.
- 결정: `BaseHooksAdapter.expireAllForMember` 를 둘로 쪼갠다 — `cancelHolds(memberId)`(보류만 `{}` 로 닫고 셸 게이트 `AbortSignal` 발화)와 `expirePendingFor(memberId, {keepTurnEndingQuestions})`(pending 행 만료 + `waiting_* → working`). 옛 이름은 둘을 순서대로 부르는 래퍼로 남긴다. 후처리 표(`SETTLE_MATRIX`)에도 `cancelHolds` 칸을 따로 두고 `restart` 행을 `pending: 'keep-ask'` 로 바꾼다. `Office.onPtyExit` 의 `exitMode === 'restart'` 경로도 같은 규칙을 쓴다(퇴근은 되살릴 세션이 없으니 그대로 전부 만료).
- 버린 대안: ① 재시작 전에 질문을 읽어 뒀다가 스폰 뒤 다시 열기(pending id 가 바뀌어 앱 카드·`[ANSWER q#n]` 이 어긋난다). ② 보류를 안 끊고 프로세스만 죽이기(hook 프로세스가 매달려 `hold-closed` 로 돌아오고, 그 경로가 다시 만료시킨다 — 결국 같은 결과에 노이즈만).
- 결과: `member.restart` 후에도 `ask_user`/`ask_parent` 카드가 그대로 있고, 답하면 되살아난 세션 큐에 `[ANSWER q#n]` 이 `[RESUMED]` 보다 먼저 들어간다(T17 큐 규칙 그대로). 허가와 TUI `AskUserQuestion` 은 예전처럼 만료된다.
- 근거: T36 기록, `test/office/AfterCare.test.ts`("restart: ask_user·ask_parent 질문은 살아남고…", "restart: 살아 있는 hook 보류는 끊는다"), PROTOCOL.md "후처리".

## D-38 · 2026-09-16 · 마이그레이션이 남긴 `pending` FK 상처는 **스키마 버전을 올리지 않고 열 때마다 자가 복구**한다
- 맥락(T39 시연): 부서를 만들고 부장에게 지시하니 팀원들이 `Write` 허가에서 **카드도 없이** 멈췄다. 원인은 T34(v1 → v2) 마이그레이션이었다 — `V1_RENAME_SQL` 의 `ALTER TABLE members RENAME TO members_v1` 이 **다른 테이블의 `REFERENCES` 절까지 고쳐 쓴다**(`PRAGMA foreign_keys = OFF` 로는 못 막는다. 그건 `legacy_alter_table` 의 몫이고, `migrate()` 주석의 가정이 틀렸다). `teams`/`members`/`tasks` 는 SCHEMA_SQL 로 새로 만들어져 멀쩡했지만 **`pending` 은 `CREATE TABLE IF NOT EXISTS` 라 v1 행이 그대로 남았고 참조만 `members_v1` 로 바뀐 뒤 그 테이블이 drop 됐다.** 그 뒤로 `createPending` 이 전부 `no such table: main.members_v1` 로 던진다 = **허가 카드·`ask_user`·`ask_parent` 가 통째로 죽는다.** 새 DB 에서는 안 나고(그래서 `:memory:` 단위 테스트 481건이 전부 통과) 마이그레이션을 거친 개발 DB 하나에서만 난다.
- 결정:
  1. **복구는 `Store.migrate()` 끝의 `repairPendingFk()`** — `sqlite_master` 의 `pending` 정의에 `members_v1` 이 보이면 FK 를 끈 트랜잭션에서 `PENDING_FK_REPAIR_SQL`(새 테이블 생성 → 행 복사 → drop → rename → 인덱스 재생성)로 갈아 끼우고 `[store] pending.member_id FK 복구…` 를 찍는다. **행은 살린다**(열린 pending 이 있을 수 있다). 멱등이라 열 때마다 검사해도 작업은 깨진 DB 에서 한 번뿐이다.
  2. **`SCHEMA_VERSION` 은 2 그대로 둔다.** 이건 새 스키마가 아니라 **옛 마이그레이션의 상처**다. 버전을 3으로 올리면 "v2 를 정상으로 만든 DB"(= 새로 만든 DB)까지 3 으로 도장을 찍어야 하고, 정작 문제는 버전이 아니라 **행의 모양**이라 버전으로는 구분되지 않는다. 판정 기준은 언제나 `sqlite_master` 의 실제 정의다.
  3. 회귀는 **마이그레이션 테스트에서 pending 을 새로 INSERT 해 본다**(`test/store/Migration.test.ts`). 예전 테스트는 "마이그레이션 전 pending **행이 살아남는지**" 만 봤고 **새로 만들 수 있는지**는 아무도 안 봤다 — 그 구멍이 이 결함을 3개 태스크(T34·T35·T36) 동안 숨겼다.
- 버린 대안: ① `SCHEMA_VERSION` 3 + 일회성 스텝(위 2의 이유, 그리고 이미 v2 로 찍힌 DB 를 다시 훑어야 해서 결국 같은 검사가 필요하다). ② `V1_RENAME_SQL` 에 `PRAGMA legacy_alter_table = ON` 만 추가 — **앞으로 마이그레이션할 v1 DB** 에는 맞지만 **이미 깨진 DB 는 못 고친다**(개발 DB 가 바로 그것이다). 복구가 v1 경로 뒤에서도 도니 예방과 치료를 한 문으로 묶었다. ③ 개발 DB 를 지우고 새로 시작 — 이벤트 682건·task 18건의 이력을 버리고, 같은 지뢰를 다음 마이그레이션에서 또 밟는다.
- 딸린 관찰(고치지 않음, T30 후보): **hook 핸들러가 `req.hold()` 뒤에 던지면 `handleHook` 의 catch 가 부르는 `req.respond(PASS_THROUGH)` 가 먹지 않는다**(HookReceiver 는 `state==='held'` 면 무시한다). 그래서 이 결함의 겉모습이 "카드가 안 뜬다" 가 아니라 "CLI 가 자기 TUI 프롬프트를 띄운 채 영원히 선다" 였다. catch 에서 `handle.resolve(PASS_THROUGH)` 까지 하고 `handler-error` 를 `daemon.notice` 로 올리는 것은 T30 에서.
- 근거: T39 기록, `src/store/schema.ts` `PENDING_FK_REPAIR_SQL`, `src/store/Store.ts` `repairPendingFk()`, `test/store/Migration.test.ts`(T39 2건), 실기 로그 `[daemon:error] hook PermissionRequest handler failed for m_0004d07c96bb: no such table: main.members_v1`.

## D-39 · 2026-09-17 · 보존 정리는 **하루에 한 번**, 지우는 것은 "끝난 이력" 뿐 — pending 30일 / task 90일 / events 부서당 50,000건
- 맥락(T30): 정리가 `Office.start()` 에서 **기동 때 한 번뿐**이었다. 그런데 이 데몬은 켜 두고 며칠씩 쓰는 물건이라(앱이 붙어 있는 동안 계속 산다) 실제로는 거의 돌지 않는다. 그리고 정리 대상이 `events` 하나뿐이라 `pending`·`tasks` 는 무한히 쌓였다 — 개발 DB 하나에서 이미 events 682행·task 22행이고, 팀을 굴리면 pending 은 허가 카드 수만큼 늘어난다.
- 결정:
  1. **주기**: 기동 때 한 번 + `setInterval(24h)`. 타이머는 **`unref()`** — 정리 타이머가 데몬을 살려 두면 안 된다. `shutdown()` 이 `clearInterval`.
  2. **대상과 기준**: `events` 부서당 최신 50,000건(기존 그대로), 닫힌 `pending`(`answered`/`expired`) **30일**, 끝난 `tasks`(`reported`/`aborted`) **90일**. 기준 시각은 pending 이 `answered_at`(없으면 `created_at`), task 가 `updated_at`.
  3. **열린 행은 절대 안 지운다** — `open` pending, `queued`/`assigned` task. 그건 이력이 아니라 **상태**다. 아무리 오래된 허가 카드라도 지우면 그 멤버의 hook 보류가 답을 못 받는다. 나이가 아니라 "닫혔는가" 가 기준이다.
  4. **왜 30 과 90인가**: pending 은 한 번 답하면 다시 볼 일이 거의 없고(사용자가 보는 이력은 `events` 의 `waiting_approval`/`asking` 줄이다) 개수가 제일 빨리 는다 — 짧게. task 는 "누가 무엇을 시켰고 무엇을 보고했나" 라 사무실의 진짜 기록이고(D-35 가 부서 삭제 때만 지우기로 못 박은 것도 그래서다) 보고서 탭이 읽는다 — 길게. `events` 는 건수 상한이 이미 있고 부서 단위라 그대로 둔다.
  5. **실패는 삼킨다**: 정리가 던져도 기동을 막지 않고 `daemon.notice{warn}` + 콘솔 경고만. 디스크·락 문제로 데몬이 안 뜨는 쪽이 훨씬 나쁘다.
- 버린 대안: ① `tasks` 도 `events` 처럼 **건수** 상한 — 부서마다 활동량이 달라 "언제 것까지 남았는지" 를 사람이 예측할 수 없다(보고서 탭이 거짓말한다). ② soft delete(`deleted_at`) — 조회마다 필터가 늘고, 정작 사람이 보는 이력은 `events` 로 충분하다(D-35 에서 이미 버린 길). ③ `VACUUM` 까지 같이 — 파일이 수십 MB 수준이라 이득이 없고 큰 DB 에서 기동을 몇 초씩 잡는다. ④ 주기를 1시간으로 — 지우는 양이 하루치도 안 되는데 쓰기 트랜잭션만 늘어난다.
- 근거: `src/store/Store.ts` `pruneRetention()`/`prunePending()`/`pruneTasks()` + `DEFAULT_RETENTION`, `src/office/Office.ts` `runRetention()`/`startRetentionTimer()`(`RETENTION_INTERVAL_MS`), `test/store/Retention.test.ts`(5건), `test/office/CrashRehire.test.ts`("보존 정리 타이머" 2건), PROTOCOL "데몬 수명·안정화 (T30)" 4.

## D-40 · 2026-09-17 · 데몬은 하나만 — `daemon.json` 의 pid 가 **살아 있고 그 ws 포트가 듣고 있으면** 기동 거부(exit 3)
- 맥락(T38 함정 ⑤ → T30): 같은 데이터 폴더를 두 데몬이 열면 SQLite 마이그레이션 중 읽기가 깨지고(T38 실기에서 실제로 봤다) hook·ws·mcp 포트가 겹쳐 절반만 뜬 데몬이 남는다. 그런데 지금까지는 나중에 뜬 데몬이 `daemon.json` 을 **조용히 덮어써** 앱·콘솔이 어느 쪽에 붙었는지도 알 수 없었다.
- 결정:
  1. 판정은 **두 조건이 모두** 맞을 때만 거부한다 — ① `daemon.json` 의 pid 가 살아 있다(`process.kill(pid, 0)`, `EPERM` 도 "살아 있음"), ② 그 `wsPort` 에 127.0.0.1 로 붙는다(400ms). 하나만으로는 안 된다: pid 만 보면 **pid 재사용**된 남의 프로세스 때문에 정상 기동을 막고, 포트만 보면 다른 프로그램이 7420 을 쥔 경우에 막는다.
  2. 거부는 **조용하지 않게** — stderr 에 pid·`taskkill /F /PID <pid>`·콘솔 `shutdown`·`daemon.json` 경로를 찍고 **exit 3**. 0(성공)도 1(일반 오류)도 아닌 코드를 준 이유는 앱 런처·스크립트가 "이미 돌고 있다" 를 실패와 구분할 수 있게 하기 위해서다.
  3. **죽은 pid 는 예전 그대로 덮어쓴다** — 크래시 뒤 재기동을 막으면 안 된다(T09 복구가 바로 그 경로다).
  4. 탈출구는 `PIXEL_FORCE_START=1` **하나뿐**이고, **통합 테스트는 이것을 쓰면 안 된다** — 테스트의 올바른 격리는 자기 `PIXEL_DATA_DIR` 이다(그게 애초에 깨졌던 이유다). 문서·오류 메시지 양쪽에 적었다.
  5. 확인은 `Office.start()` 맨 앞(포트를 열기 전, `daemon.json` 을 쓰기 전)에서 한다 — 거부된 데몬은 살아 있는 데몬의 `daemon.json` 을 **건드리지 않는다**.
- 버린 대안: ① 파일 락(`.lock`) — 하드 킬되면 유령 락이 남아 "죽은 pid 는 덮어쓴다" 를 다시 손으로 구현해야 한다. ② 포트 bind 실패에 기대기(EADDRINUSE) — 그때는 이미 Store 를 열어 DB 를 건드린 뒤고, 오류 메시지가 "누가 쓰고 있는지" 를 말해 주지 않는다. ③ 무조건 거부(pid 살아 있기만 하면) — pid 재사용으로 기동 못 하는 사고가 재기동 실패보다 나쁘다.
- 근거: `src/office/singleton.ts`(`assertSingleDaemon`, `DaemonAlreadyRunningError`), `src/index.ts`, `test/office/Singleton.test.ts`(9건), 실기: 두 번째 데몬 기동 → exit 3 + pid 26364 메시지(T30 기록).

## D-41 · 2026-09-17 · 이벤트 코얼레스는 **하지 않는다** — 실측 부서당 31.3건/분, 피크 1초에 6건
- 맥락(T30 "남은 것" → T31): 03 의 T30 줄에 "이벤트 코얼레스 점검" 이 있었다. T30 실기는 부장 하나뿐이라(한 턴 5~7건) 판단 근거가 없어 **"T31 시연에서 부서 하나에 팀 여럿을 돌려 실제 초당 이벤트 수를 본 뒤 판단"** 으로 미뤘다. 그래서 T31 에서 일부러 제일 시끄러운 모양을 만들었다 — 부서 1 · 팀 2 · **멤버 7명**(부장 + 팀장 2 + 팀원 4)이 동시에 hire → delegate → 셸 쓰기 → 보고 상향까지 한 바퀴.
- 실측(seq 711~798, 168.8초, 부서 t31):

  | 지표 | 값 |
  |---|---|
  | 전체 | 88건 / 168.8초 = **31.3건/분** |
  | 분당(UTC) | 01:09 42건 · 01:10 6건 · 01:11 40건 |
  | 멤버별 최다 | 부장 17건(6.0/분), B장 17건, A장 15건 … 최소 A장1 8건 |
  | 피크 | 임의의 **1초 창 6건** / 5초 창 17건 (부서 전체) |
  | 멤버별 피크 | **전원 1초 창 2건** |
  | 같은 멤버·같은 종류가 1초 안에 연달아 | 6건 / 88건 (6.8%) |
- 결정: **코얼레스(합치기)를 넣지 않는다.** 근거 셋.
  1. **양이 문제가 아니다.** 부서 전체 31.3건/분이면 앱 링버퍼 2,000건을 채우는 데 **64분**이고, 피크도 1초에 6건이다. 위젯 재빌드·로그 탭 스크롤·WS 트래픽 어디에도 부담이 없다(실기 내내 앱이 매끄러웠다). 멤버 한 명이 보내는 양은 **1초에 최대 2건**이다.
  2. **합칠 만한 후보 6건이 전부 합치면 안 되는 것들이었다.** 3건은 `running{tool:'mcp__team__create_team'}`·`hire` 가 0.6~0.8초 간격으로 **두 번 불린 것**(진짜 도구 호출 두 번 — 하나로 줄이면 "팀원 둘을 고용했다" 가 하나로 보인다). 나머지 3건은 `running{cmd}` **0ms** 뒤의 `running{waiting:'shell-lock'}` 인데, 이건 "시작했다 → 막혔다" 라는 **서로 다른 사실**이고 앱 말풍선 `⏳ 셸 대기 중 (락: …)` 이 바로 이 두 번째 이벤트다(T29 결함 ③ → T37 수정 → T39·T31 실기 확인). 합치면 방금 고친 화면이 도로 망가진다.
  3. **합치면 잃는 것이 크다.** `seq` 는 재접속 replay(`hello{since}`)·`events.query` 백필(D-21)·중복 제거의 기준이라, 합치는 순간 "어디까지 봤나" 가 이벤트 개수와 어긋난다. 게다가 이벤트는 사무실 애니메이션의 입력이기도 하다(말풍선·모니터·이동) — 시간 축을 뭉개면 움직임이 건너뛴다.
- 버린 대안: ① 데몬에서 같은 멤버·같은 kind 를 N ms 창으로 합치기 — 위 2·3. ② 앱에서만 렌더 스로틀(16ms 배치 repaint) — 지금 부담이 없어 넣을 이유가 없고, 넣는 순간 위젯 테스트 30여 개의 기대가 바뀐다. ③ `term` 처럼 replay 대상에서 빼기 — 이벤트는 사무실의 유일한 이력이라 불가.
- 되돌릴 조건(다음에 다시 볼 기준선): **부서 하나가 5분 평균 300건/분을 넘거나**, 임의의 1초 창이 **50건**을 넘거나, 앱 프레임이 눈에 띄게 끊기면. 그때는 "합치기" 보다 **앱 쪽 렌더 배치**(데몬 이벤트·seq 는 그대로 두고 repaint 만 모으기)를 먼저 본다.
- 근거: T31 실기(멀티 팀 버스트), 원본은 DB `events` seq 711~798(부서 `d_a9b2bb39645d`). 같은 실기가 성공 기준 9(셸 뮤텍스)도 겸했다.

## D-42 · 2026-09-17 · 레이아웃 v2 — 시선 서열·pending 주인 하나·범례 7칸·규모 규칙
- 맥락(T32 디자인 리뷰): 01 은 사무실 배치를 "부서 = 탭, 부장 상단, 팀 클러스터, 내 책상은 부장 것만" 한 줄로 M6 에 미뤘고 T37 이 그대로 구현했다. 리뷰(목업 3종 + Opus 외부 의견 + 7패스)에서 규모·상태·터미널 폭·인박스가 비어 있음이 드러났다(시작 5/10).
- 결정(전문은 `docs/design/레이아웃-v2.md`, 여기엔 뒤집힐 수 있는 축만):
  1. **시선 서열** `허가 카드 > 내 책상 대기 수 > 부장 > 팀 클러스터 > 로그·터미널`. 부장 앵커는 크기가 아니라 **금색 카펫 + 왕관**(D3: 부장 책상은 일반 크기).
  2. **pending 의 주인은 오른쪽 패널 전역 인박스 하나**(선택 멤버 무관, 2장 펼침 + "+N"). 사무실 내 책상의 슬롯 4칸·"+N" 배지는 같은 목록의 그림. `ask_parent` 안내 카드는 그 멤버 패널에만.
  3. **상태 13종 → 범례 7칸**(작업·한가·보고대기·**내 차례(주황 ❗)**·대기(노랑 ⏳)·오류·퇴근) — 링 색·범례·패널 상태 점·T33 포즈의 단일 기준.
  4. **규모**: 사무실은 세로 스크롤 + 내 책상·범례는 바닥 고정 바, scale 0.6~1.5, 폭 ≥1600 이면 5열, 클러스터 폭 = min(n,4)열 왼쪽 정렬·한 줄 나란히. 패널 폭 드래그 420~720(기본 480), 터미널 탭 열면 660 자동, `Ctrl+T` 전체 폭.
  5. 글자 빼기: "책상 N ·" 접두어와 지시 바 드롭다운 삭제(정적 칩 "♛ 부장에게"). 말풍선 상시 표시는 alert 만. 보고서 탭은 카드 스택이 아니라 문서 흐름.
- 버린 대안: 큰 부장 방(C안 — 세로 공간, D3), 6칸 범례(내 차례와 기다림을 한 색에 — 행동 지침이 안 됨), 두 줄 대기 슬롯(내 책상이 커짐), 패널 = 멤버 목록(B안 — 사무실이 이미 목록), 임의 멤버 지시(rev 3 위반).
- 결과·근거: 리뷰 5 → 9/10, 미결 0. 이슈 9 이후는 사용자 지시("10/10 선택지로만 진행, 실사용에서 수정 나온다")로 권장안 자동 채택. 구현은 T40-1~6.

## D-43 · 2026-09-17 · 렌더러는 CustomPainter 유지, 스프라이트는 32×32 정수 배율, 에셋은 CC0·서체는 OFL
- 맥락(T12 가 M6 로 미룬 "Flame 여부" + T33 이 받을 규격): 스프라이트 없이 원으로 그려 온 캐릭터를 픽셀아트로 바꾸려면 셀 크기·포즈·배율·에셋 출처가 먼저 정해져야 한다.
- 결정:
  1. **CustomPainter 유지.** 스프라이트는 아틀라스 `drawImageRect`, 애니메이션은 기존 `office_motion` 틱. Flame 은 카메라·씬 그래프가 필요해질 때(줌·장식, v2) 재검토.
  2. **셀 32×32, 아틀라스 8열, 포즈 8종**(idle·type·think·alert·ask·report·error·gone) + 걷기 4프레임. 13상태 → 8포즈 매핑은 레이아웃-v2 패스 7 표.
  3. **정수 배율만**: `spriteScale = scale >= 0.75 ? 2 : 1`, `FilterQuality.none`. 레이아웃 scale(연속)과 분리 — 픽셀아트가 뭉개지지 않게.
  4. **에셋은 CC0 우선(Kenney)**, 포즈는 직접 편집. CC-BY 는 LICENSE 기록 조건으로만. **AI 생성 스프라이트는 쓰지 않는다**(프레임 일관성·라이선스 불명). 서체 Galmuri11 / Pretendard / D2Coding — 전부 OFL 1.1, 파일 동봉 + LICENSE.
  5. 외형: 엔진별 셔츠 색(Claude `#E0956E`, Codex `#8FB4FF`) + 이름 해시 머리색 4종. 직급은 왕관/별 배지로만.
- 버린 대안: Flame 전환(렌더러 두 벌, 지금 필요 없음), 16×16 셀(모니터·배지와 같이 놓기엔 작다), 연속 배율(흐림), AI 생성 스프라이트.
- 결과·근거: T33 착수 조건. 실사용에서 포즈가 부족하면 8종에 append.

## D-44 · 2026-09-17 · 스프라이트는 CC0 팩이 아니라 **저장소 안의 결정적 픽셀 코드**로 생성한다 (D-43 §4 수정)
- 맥락(T33): D-43 §4 는 "CC0 우선(Kenney), 포즈는 직접 편집" 이었다. 실제로 받아 본 Kenney **Roguelike Characters 2.0** 은 서 있는 한 포즈짜리 페이퍼돌(몸 + 장비 레이어)이라 **걷기 프레임도, 앉기·타이핑·보고 포즈도 없고**, Tiny Town 에는 캐릭터가 없다. D-43 §2 가 요구하는 8포즈 + 걷기 4프레임을 "편집" 으로 얻으려면 전부 다시 그려야 했다.
- 결정: `dev/app/tool/gen_sprites.dart`(순수 Dart, 의존성 0, 자체 PNG 인코더)가 손으로 적은 픽셀 좌표에서 `assets/sprites/characters.png`(256×96, 32×32 셀 8열 + 걷기 2줄)를 생성한다. 몸·머리·팔·다리 함수를 공유하고 외곽선은 실루엣에서 자동, 걷기-오른쪽은 왼쪽의 거울 — **프레임 일관성이 구조적으로 보장**된다. 산출물은 이 저장소 것이라 **CC0 1.0** 으로 둔다. D-43 의 "AI 생성 스프라이트 금지" 는 그대로다(이건 이미지 생성이 아니라 결정적 도형 코드). 나머지 D-43 조항(CustomPainter · 32×32/8열 · 정수 배율 + `FilterQuality.none` · 엔진 셔츠 색 + 이름 해시 머리색 · 왕관/별만)은 그대로.
- 딸린 결정: 캔버스의 범례·직급 아이콘은 글리프가 아니라 **아틀라스에서 그린 아이콘**. 이유: 세 서체 cmap 을 직접 읽어 보니 `📨` `⏳` 는 셋 중 어디에도 없고 `♛` 는 D2Coding 에만, `❗` `❓` 는 Galmuri11 에만 있었다(T40 편차 ⑤ "tofu" 의 진짜 원인은 시스템 폴백 의존). 남는 글리프는 `✉ ◷ ◫ ▤ ❝` 로 바꾸고, 번들 서체에 없는 글리프를 막는 allow-list 테스트를 둔다. 상태 링은 머리 옆이 아니라 **발 밑**(스프라이트 머리가 좁아 목걸이처럼 보였다).
- 버린 대안: Kenney 페이퍼돌 위에 포즈를 덧그리기(사실상 전부 새로 그리는 것과 같다), 유료 팩(LimeZu 등 — 재배포 제약), AI 이미지 생성(D-43 금지 유지).
- 결과·근거: 8× 확대 캡처(`img/T33-11-zoom8x-noblur.png`)에서 모든 픽셀이 깨끗한 2×2 블록. 테스트 344 → 410(T33 단독), 머지 후 431. 픽셀 스타일이 마음에 안 들면 생성기의 좌표만 고치면 된다 — 에셋 교체가 아니라 코드 수정.
