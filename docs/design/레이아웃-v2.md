# 레이아웃 v2 — 직무 체계 rev 3 사무실 (T32 디자인 리뷰)

- 날짜: 2026-09-17
- 상태: **리뷰 진행 중** (이 문서가 T32 의 산출물이자 리뷰 대상 "플랜"이다. 확정되면 상태를 APPROVED 로 바꾸고 03/04 에 반영)
- 근거: 01-설계문서 §3 Flutter 데스크탑 · §직무 체계 rev 3 "사무실 배치" 한 줄 · T37 구현(`dev/app/lib/office/`) · T39 캡처 17장 · `office-sketch.html`(v1)
- 결정 번호: 이 리뷰에서 확정한 것은 04-결정기록 D-42 이후로 append

## 0. 리뷰 시작 시점의 플랜 (있는 그대로)

설계문서가 M6 로 미룬 문장은 하나다:

> **사무실 배치**(M6에서 확정): 부서 = 탭, 부장 책상 상단, 팀별 책상 클러스터, 내 책상은 부장 보고·질문·허가 카드만 받는다.

T37 이 이것을 그대로 구현했다(부장 책상 맨 위 가운데 → 팀 클러스터 상자(제목 줄 + 한 줄 4책상) → 내 책상 아래 가운데, 문은 왼쪽 세로 중앙, 오른쪽 패널 420px 고정, 지시 바 대상 = 부장 고정). 너비 900 미만이면 축소(하한 0.45), 클러스터가 많아 내 책상과 겹쳐도 세로 축소만.

**리뷰 시작 시 완성도: 5/10.** 구조는 정해졌지만 규모 대응·터미널 폭·스프라이트 규격·상태 범례·인박스 배선·첫 실행 여정이 비어 있다(아래 패스에서 채운다).

## 1. 무엇이 이미 있나 (재사용)

| 자산 | 위치 | 그대로 쓰는 것 |
|---|---|---|
| 배치 기하 | `office_layout.dart` (`OfficeDeskPlan` → 책상 Rect·클러스터 상자·줄·문·내 책상) | 책상 번호 규칙(부장 0 → 클러스터 순), `visitorSpot`(ask_parent 방문), `queueSlot` |
| 색 팔레트 | `office_painter.dart` `OfficeColors` | 바닥 `#1B1F2A` · 책상 `#2A3042` · 모니터 초록 `#9BE7A1` · 부장 금 `#FFD166` · 팀장 은 `#BFC7DA` · 작업 파랑 `#6C8EFF` · 한가 초록 `#7ED3A1` · 대기 노랑 `#FFC857` · 오류 빨강 `#FF6B6B` · 내 책상 보라 테두리 `#C9B6FF` |
| 상태 3중 표현 | 스프라이트 자리(원) + 모니터 마지막 줄 + 말풍선 | 그대로. 원 → 스프라이트로 바뀌는 것이 T33 |
| 이동 | `office_motion.dart` (책상↔내 책상↔상사 책상, 보고 방문 6초, 문 입장·퇴장) | 그대로 |
| 오른쪽 패널 | `right_panel.dart` (헤더 트리 줄 · 카드 · 탭 4종 로그/터미널/지시문/보고서) | 탭 구성 |
| 카드 | `pending_card.dart` (허가/질문/ask_parent 안내 + "대신 답하기", `PendingInbox` 는 **미배선**) | 카드 UI |
| 상단 바 | `top_bar.dart` (부서 탭 · 선택 멤버 배지 · 데몬 pill · 멤버 N·대기 N · 부서 만들기 · ⋮) | 그대로 |
| 지시 바 | `command_bar.dart` (부장 고정 드롭다운 · 입력 · 중단 · 전송) | 입력·중단·전송 |
| 빈 상태 | `emptyOfficeHint` "부서 만들기로 부장을 임명하세요", 패널 "캐릭터를 선택하세요", 보고서 "아직 보고 없음", 데몬 없음 오버레이 | 문구는 재검토 |

## 2. 기준 그림 (목업, 2026-09-17 승인)

디자인 도구로 변형 3종(A·B·C)을 만들어 비교 보드에서 골랐다. 목업 이미지는 AI 생성물이라 이 공개 스냅샷에는 싣지 않는다.

**결정(사용자, D2·D3): 큰 틀은 A, 말풍선·라벨은 한글, UI 요소는 셋을 섞는다. 부장 자리는 일반 책상 크기.**

| 가져오는 것 | 출처 | 지금 앱과의 차이 |
|---|---|---|
| 팀마다 다른 색 카펫 클러스터, 제목 태그는 상자 왼쪽 위에 걸침, 제목 색 = 카펫 색 | A + B | 지금은 회색 선 상자 + 회색 제목 |
| 모니터 2줄(위: 명령/도구, 아래: 결과 요약) | B | 지금은 1줄 |
| 캐릭터 상태 = 스프라이트 + 책상 아래 상태 점(색) + 오류 시 책상 테두리 빨강 | A + C | 지금은 원 색·링 |
| 내 책상 = 인박스: 안쪽에 대기 슬롯(빈 슬롯 점선 실루엣) + 대기 수 배지 | A + C | 지금은 텍스트 목록 + 위쪽 줄 |
| 하단 상태 범례 한 줄(작업·한가·대기·주의·오류·퇴근) | A | 없음 |
| 오른쪽 패널 헤더: 아바타 + 이름 + 엔진 칩 + 상태 + 트리 줄 + cwd | A | 아바타만 없음 |
| **허가 대기 블록은 선택 멤버와 무관하게 항상 보인다** | B | 지금은 그 멤버를 선택해야 카드가 보임(결함 ⑤) |
| 터미널 탭은 80열이 들어가는 폭 | A | 420px 고정(≈55열) |
| 부서 탭 = 폴더 아이콘 + 이름 | C | 이름만 |
| 맨 아래 단축키 힌트 줄 | A | 없음 |

**안 가져오는 것:** B 의 "지시 대상 = 임의 멤버"(rev 3 위반 — 지시는 부장에게만), B 의 "패널 = 멤버 목록"(사무실이 이미 목록이다), C 의 큰 부장 방(세로 공간 — D3 에서 일반 크기로).

## 3. 리뷰 패스

외부 의견은 다른 모델(서브에이전트) 하나로 받았다(Codex 는 D-23 의 사용량 한도). 하드리젝션 2건(보고서 카드 스택 · 오버레이 예외 문자열)은 패스 4 에서, 나머지는 해당 패스의 선행 항목.

### 패스 1 — 정보 구조 (시작 4/10)

**시선 서열(확정, D5=1A):** `허가 카드 > 내 책상 대기 수 > 부장 > 팀 클러스터 > 로그·터미널`. 화면 어느 요소든 이 서열보다 위로 올라가면 안 된다 — 아래 모든 결정의 기준.

**부장 앵커(확정, D5=1A):** 책상 크기는 일반과 같다(D3). 대신 **바닥과 관**으로 구별한다 — 부장 책상 아래 금색 카펫(책상 Rect 사방 +20px, `#FFD166` 알파 0.12, 모서리 4px), 책상 위 가운데 왕관 스프라이트 16×16(T33), 캐릭터 링 금색은 유지. 팀장은 카펫 없이 은색 별(현행).

```
┌ 상단 바: [폴더] 부서탭… │ 선택 멤버 │ 데몬 pill │ 멤버 N·대기 N │ [부서 만들기] ⋮ ┐
├──────────────── 사무실(왼쪽, 나머지 폭) ────────────────┬── 오른쪽 패널 ──┤
│  문     ┌─금색 카펫─┐                                   │ 헤더(아바타·이름·엔진·상태·트리 줄·cwd)
│  │      │  부장 책상 │ ← 서열 3                          │ ▶ 허가/질문 대기 블록 (전역, 서열 1)
│         └──────────┘                                    │ 탭: 로그 │ 터미널 │ 지시문 │ 보고서
│  ┌ 팀 카펫 A ── 팀장★ 팀원 팀원 팀원 ┐  ← 서열 4          │
│  ┌ 팀 카펫 B ── 팀장★ 팀원 ┐                             │
│  …(세로 스크롤, 패스 6)                                   │
│  [내 책상 · 대기 N] 슬롯 ○ ○ ◌ ◌   ← 서열 2 (바닥 고정)   │
│  범례: ●작업 ●한가 ●대기 ●보고대기 ●오류 ●퇴근              │
├──────────── 지시 바: 부장에게 │ 입력 │ 중단 │ 전송 ────────┴─────────────────┤
```

**pending 의 주인은 하나(확정, D6=2A):** 오른쪽 패널 헤더 바로 아래 **"내 책상 · 대기 N" 블록**이 사용자 몫 pending(`Pending.goesToUser` — 허가 전부 + 부장 `ask_user` + TUI 질문) 전부를 **선택 멤버와 무관하게** 오래된 순서로 보여 준다(`PendingInbox` 배선). 카드 2개까지 펼치고 3개째부터는 접힌 목록 "+N" (클릭하면 펼침). 사무실 내 책상의 슬롯·배지는 같은 목록의 그림이다 — 슬롯 클릭 = 그 멤버 선택 + 인박스에서 그 카드로 스크롤. 카드에 답하면 양쪽에서 동시에 사라진다. 선택 멤버의 `ask_parent` 안내 카드("대신 답하기")는 사용자 몫이 아니므로 지금처럼 그 멤버 패널(인박스 아래, 탭 위)에만.

**글자 빼기(확정, D7=3A):** 책상 라벨은 **이름(굵게) + 직급 배지 + 엔진 배지**만 — "책상 N ·" 접두어 삭제(번호는 툴팁·로그에만). 축소 시 숨기는 순서: 엔진 배지 → 직급 배지 글자(아이콘만) → 이름은 마지막까지(말줄임). 지시 바의 드롭다운은 **정적 칩 "♛ <부장이름>에게"** 로 교체(부장 없으면 회색 "부장 없음" + 입력 비활성); 부서 전환은 상단 탭이 한다.

**패스 1 재평가: 9/10** — 서열·앵커·pending 주인·글자 빼기가 확정. 남은 1점은 보고서 탭 타이포(패스 4 하드리젝션에서 다룸).

### 패스 2 — 상태 커버리지 (시작 3/10)

코드에는 상태 어휘 13종이 이미 있는데(`office_scene.dart`: starting · working(reading/editing/running/thinking) · idle · free · waiting_reports · waiting_approval · waiting_answer · ask_parent 방문 · delegating · reporting · exited · error · 셸 락 대기) 플랜에는 상태 명세가 0줄이고 범례는 6칸이다.

**상태 → 범례 7칸 매핑(확정, D8=4A).** 이 표가 캐릭터 링 색 · 하단 범례 · 패널 헤더 상태 점 · T33 포즈의 **단일 기준**이다.

| 범례 칸 | 색 | 아이콘 | 포함하는 코드 상태 | 사용자가 할 일 |
|---|---|---|---|---|
| 작업 | 파랑 `#6C8EFF` (현행 charWorking) | — | working(reading·editing·running·thinking) · delegating · reporting | 없음 |
| 한가 | 초록 `#7ED3A1` (charIdle) | — | idle · free | 지시 가능(부장만) |
| 보고 대기 | 초록 **점선** 링 | 📨 | waiting_reports (부하 보고를 기다림) | 없음 |
| **내 차례** | 주황 `#FF9F43` (신규) | ❗ | waiting_approval · 부장 `ask_user` · TUI AskUserQuestion | **답해야 한다** — 내 책상 슬롯에 선다 |
| 대기 | 노랑 `#FFC857` (charWaiting) | ⏳ | `ask_parent` 답 대기(상사 책상 옆) · 셸 락 대기 · starting(출근 중) | 없음 |
| 오류 | 빨강 `#FF6B6B` (charError) + 책상 테두리 빨강 | ⚠ | error(비정상 종료) | 재고용 배너 |
| 퇴근 | 회색 `#474D5E` (charGone), 의자만 | — | exited | 없음 |

범례는 사무실 캔버스 맨 아래 한 줄(높이 24px, 글자 11px, 점 8px)로 항상 보인다. 색 점 = 캐릭터 링 색과 동일.

**같은 자리에 여러 명(확정, D9=5A):**
- 내 책상 = 인박스 그림. 안쪽에 **슬롯 4칸 고정**(빈 슬롯은 점선 실루엣), 오래된 것부터 왼쪽. 5명째부터는 캐릭터가 자기 책상에 남고(링 주황 ❗) 맨 오른쪽 슬롯 위 배지 **"+N"**. 배지 클릭 = 인박스 펼침(D6). 헤더 "내 책상 · 대기 N" 의 N 은 전체 수.
- 상사 책상 옆(`ask_parent`): `visitorSpot(deskIndex, k)` — k 번째는 오른쪽으로 `2r + 8px` 씩, **최대 2명** 표시, 3명째부터 두 번째 자리 위 "+N" 말풍선. 책상이 화면 오른쪽 끝(다음 자리가 `size.width - r - 2` 를 넘으면)이면 왼쪽 옆으로 접는다.

**로딩 3종 + 만료(확정, D10=6A):**
- 데몬 pill 3상태: 초록 `● 데몬 v1.0 · pid N` / 노랑 점멸(1Hz) `◌ 연결 중 · N초` / 빨강 `● 끊김 · 재시도 N회`. 끊김 오버레이는 같은 문구 + 다음 재시도까지의 진행 바(재접속 백오프 주기). 예외 문자열은 **"자세히" 접힘**(패스 4 하드리젝션 ②).
- 멤버 `starting`: 문이 열리고 캐릭터가 책상까지 **1.2초** 걸어가 앉는다(기존 `office_motion` 경로 재사용). 모니터 `(출근 중)` + 노란 링(대기 칸). 프롬프트 준비(`isPromptReady`)되면 초록.
- 복구(`[RESUMED]`): 복구된 멤버는 책상 테두리 **점선 3초** + 말풍선 `↻ 복구됨`. 복구로 만료된 허가는 인박스에 회색 **"만료 — 재지시"** 카드(현행 redo 카드가 인박스 안으로 들어온다).
- 허가·질문 카드 오른쪽 위 메타: `요청 2분 전 · 내일 10:32 만료`(hook 보류 상한 86400초 기준). 만료 1시간 전부터 메타 글자 초록 → 주황. 만료되면 카드가 회색 "만료 — 재지시" 로 바뀐다.

**빈 상태 3종 + 퇴근 누적(확정, D11=7A):**
- 부서 0개: 빈 바닥 + 문만. 가운데 큰 버튼 **"부서 만들기"**(상단 버튼과 같은 다이얼로그) + 한 줄 "프로젝트 폴더 하나 = 부서 하나. 부장이 팀을 꾸립니다." 회색 격자 위 문구 한 줄로 끝내지 않는다.
- 부장만(팀 0): 부장 카펫 아래 **점선 클러스터 자리 하나** "팀은 부장이 만듭니다 — 아래 지시 바에 첫 지시를". 지시 바 placeholder 는 예시 문장 "예: 이 저장소 구조를 파악해서 보고해".
- 클러스터 상자 폭 = `min(인원, 4)` 열 폭 + inset, **왼쪽 정렬** (1명 팀이 전폭 빈 상자가 되지 않게). 같은 줄에 두 클러스터가 들어가면 나란히 놓는다(패스 6 규모).
- 퇴근 책상: 팀이 살아 있는 동안 **의자만 남긴 회색 책상** 으로 **10분** 유지 → 그 뒤 클러스터 제목의 **"퇴근 N" 배지**로 접힌다(배지 클릭 = 펼침, 앱 로컬 상태). 전원 퇴근 팀은 제목 줄만 남긴 낮은 상자 `팀 셀 · 전원 퇴근 · 보고 3건`(클릭 = 보고서). 팀 행 삭제(`dismiss_team`·부서 삭제)는 지금처럼 상자 자체가 사라진다.

**상태 표(패스 2 산출물):**

| 기능 | 로딩 | 비어 있음 | 오류 | 성공 | 부분 |
|---|---|---|---|---|---|
| 사무실 캔버스 | 데몬 pill 노랑 + 오버레이 진행 바 | 부서 0: 큰 "부서 만들기" 버튼 / 부장만: 점선 팀 자리 | 끊김 오버레이(문구 + 자세히 접힘) | — | 일부 퇴근: 의자만 회색 → 10분 뒤 "퇴근 N" 배지 |
| 캐릭터 | 출근 중: 문→책상 1.2초, 노란 링 | — | 오류 포즈: 빨간 링 + 책상 테두리 빨강 + `⚠` 말풍선 + 패널 재고용 배너 | 보고: 내 책상 방문 6초 `📄 보고` | ask_parent 대기: 상사 옆, 셸 락: `⏳ 셸 대기 중 (락: X)` |
| 내 책상 · 인박스 | — | 슬롯 4칸 점선, "대기 없음" | 만료 카드 회색 "만료 — 재지시" | 답하면 양쪽에서 동시에 사라짐 | 5명째부터 "+N" |
| 허가·질문 카드 | — | — | 응답 실패(-32xxx) 문구를 카드 안에 | 카드 사라짐 + 캐릭터 복귀 | 메타 "요청 N분 전 · 만료 시각", 1시간 전 주황 |
| 오른쪽 패널 | 터미널 attach 중 "붙이는 중…" | "캐릭터를 선택하세요" → 인박스는 그대로 보임 | 멤버 사라짐 배너(현행) | — | 부장이 아니면 "지시는 부장에게" 힌트(현행) |
| 보고서 탭 | — | "아직 보고 없음 — 부장이 보고하면 여기에" | — | 새 보고 도착 시 탭 배지(패스 3) | — |
| 지시 바 | 전송 중 스피너 200ms | 부장 없음: 회색 칩 + 비활성 | -32004 문구를 입력 위에(현행) | 입력 비움 | 작업 중 전송 = "큐에 넣음" 힌트(InputQueue) |

**패스 2 재평가: 9/10** — 13→7 매핑·다중 대기·로딩·빈 상태·표까지 확정. 남은 1점: 보고 도착 순간(패스 3).

### 패스 3 — 사용자 여정 (시작 4/10)

첫 실행 스토리보드가 문서에 없고, T39-8(첫 화면 = 스택 트레이스)·T39-15(첫 허가 = 6줄 명령 뒤범벅)가 첫 5초·첫 5분을 망친다. 5초(본능)·5분(행동)·5년(관계)을 각각 본다.

**허가 카드(확정, D12=8A) — 하루 수십 번 하는 행동은 생각 없이:**
1. 첫 줄 `❗ <도구> · <대상 마지막 조각> <동사>` — 예 `❗ PowerShell · demo39-c.txt 쓰기`. 동사는 도구별 추출 규칙(Write/Edit → 쓰기·수정, Bash/PowerShell 은 첫 토큰과 리다이렉션/`Set-Content`/`rm`·`Remove-Item`/`git push` 패턴 → 실행·쓰기·삭제·푸시; 못 하면 첫 토큰 그대로). **위험 패턴**(`rm -rf`, `Remove-Item -Recurse`, `git push --force`, `del /s`, `format`)은 첫 줄 배경 빨강 틴트(`#FF6B6B` 알파 0.15) + 태그 `위험`.
2. 둘째 줄: CLI 가 준 설명(`description`) 가변폭 13px, 없으면 생략.
3. 셋째: 명령 고정폭 12px **3줄 클램프** + "전체 보기"(펼치면 전문·스크롤).
4. 버튼: **허가**(채움 초록) · **거부**(테두리 빨강) 를 크게(높이 32), "이번 세션 항상 허가" · "수정해서 허가" 는 오른쪽 끝 작은 텍스트 버튼. 질문 카드도 같은 골격(옵션 버튼 = 주, 자유 입력 = 보조).
5. 메타(오른쪽 위): `요청 2분 전 · 내일 10:32 만료`(D10).

> 이 아래 결정(이슈 9~)은 사용자 지시("completeness 10/10 선택지로만 진행")에 따라 **권장안을 자동 선택**했다. 실제 사용에서 수정이 나온다는 전제.

**보고 도착 순간(자동 확정, 이슈 9=A):** 부장의 `report` 가 도착하면 ① 부장이 내 책상으로 걸어와 `📄 보고` 6초(현행) ② 보고서 탭에 **미확인 배지 N**(탭 라벨 옆 점 + 수), 상단 바 "대기 N" 옆에 `보고 N`(클릭 = 부장 선택 + 보고서 탭) ③ 보고서 탭을 열면 배지가 지워진다(앱 로컬 "읽음"). 인박스와 달리 **보고는 답할 것이 없으므로 카드가 아니라 배지**. 5년 관계: 보고 이력은 부서에 남는다(D-35) — 보고서 탭 상단에 "task#12 · 2026-09-16" 식 날짜 구분선.

**첫 실행 스토리보드(패스 3 산출물):**

| 단계 | 사용자 행동 | 느낌(목표) | 플랜이 보장하는 것 |
|---|---|---|---|
| 1 | 앱 실행, 데몬 없음 | "뭘 누르면 되는지 안다" | 오버레이 한 문장 + 주 버튼 **데몬 시작** 하나, 예외는 접힘(D10) |
| 2 | 데몬 시작 클릭 | "진행 중이다" | pill 노랑 점멸 + 진행 바, 3초 내 초록 |
| 3 | 빈 사무실 | "다음은 부서" | 가운데 큰 "부서 만들기" + 한 줄(D11) |
| 4 | 부서 만들기 → 부장 임명 | "누가 들어온다" | 문 열림 → 1.2초 걸어와 앉음, 모니터 "(출근 중)"(D10) |
| 5 | 첫 지시 | "무슨 말을 해야 하지" 없음 | 지시 바 placeholder 예시 문장 + 칩 "♛ 부장에게"(D7·D11) |
| 6 | 부장이 팀을 만든다 | "위계가 보인다" | 카펫 색 클러스터가 부장 아래 생김, 팀장 ★ |
| 7 | 첫 허가 | "생각 없이 허가" | 요약 줄 + 주 버튼 2(D12), 인박스 전역(D6) |
| 8 | 첫 보고 | "끝났다" | 📄 방문 + 보고서 배지(이슈 9) |
| 9 | 창 닫고 다음날 | "어제 그대로" | 스냅샷 복원 + 복구 점선 3초 + 만료 카드(D10) |

**패스 3 재평가: 9/10.**

### 패스 4 — AI 슬롭 위험 (시작 5/10)

**분류: OPERATE(앱 UI).** 시각 표현은 사무실 캔버스 안에서만, 크롬은 조용하게.

하드리젝션 처리:
- ① **보고서 탭 = 카드 스택 → 문서 흐름으로(자동 확정, 이슈 10=A).** 보고 하나 = 헤더 줄 `보고 · 부장 · 23:08 · task#12 · done`(11px, 회색) + 본문 **가변폭 14px / 행간 1.6**, 코드·경로만 고정폭, 6줄 클램프 + "펼치기", 보고 사이는 1px 구분선. 테두리 카드 없음. 지시(`[TASK]`)는 오른쪽 정렬 인용 블록(왼쪽 2px 보라 선 대신 배경 틴트).
- ② 오버레이 예외 문자열 → D10 에서 "자세히" 접힘으로 해결.

리트머스(플랜 기준): 1 제품 분명 **YES**(픽셀 사무실 + 카펫·왕관) · 2 앵커 **YES**(부장 금색 카펫, D5) · 3 라벨만으로 이해 **YES**(이름·직급·범례 7칸) · 4 영역마다 한 가지 일 **YES**(pending 주인 하나 D6, 드롭다운 제거 D7) · 5 카드 필요 **YES — 허가·질문만**(보고서는 문서) · 6 모션이 위계를 돕나 **YES 조건부**: 카드·배지는 즉시, 걷기는 장식(0.6~1.2초, 절대 카드를 지연시키지 않음) · 7 그림자·글로우 없이 고급 **YES**(플랫 유지, 그림자 0, 글로우 0).

**구체성(자동 확정, 이슈 11=A) — "색 카펫"을 숫자로:**
- 팀 카펫 6색 토큰(순환): `#2F4A3A` 초록 · `#2F3D5A` 남 · `#4A3A2F` 갈 · `#432F4A` 자 · `#2F4A4A` 청록 · `#4A2F38` 적갈 (전부 명도 ≈24%, 바닥 `#1B1F2A` 위 불투명). 제목 태그 = 같은 색을 40% 밝힌 값, 상자 왼쪽 위에 -8px 걸침, 글자 12px 굵게.
- 서체: 캔버스 라벨·말풍선·모니터 = **Galmuri11**(한글 픽셀 서체, OFL) / 패널·상단 바 = **Pretendard**(OFL) / 터미널·명령 = **D2Coding**(OFL, 한글 고정폭). system-ui 를 표시 서체로 쓰지 않는다. 라이선스 3종 모두 OFL 1.1 — 재배포 가능(T33 에서 파일 동봉·LICENSE 기록).
- 모니터 2줄: 각 줄 9px, 최대 22자(현 `cmdMaxChars=40` 을 2×22 로), scale < 0.7 이면 둘째 줄 숨김.
- 말풍선: `bubbleMaxChars` 28, 최대 폭 180×scale, 2줄. **상시 표시는 alert(내 차례·대기·오류·보고)만**, 작업 말풍선은 선택 멤버 + 호버 시에만 — 8명 화면이 말풍선 밭이 되지 않게.
- 곡률 4px 단일, 그림자 없음, 글로우 없음, 그라데이션 없음. 아이콘은 범례 7칸의 ❗⏳📨⚠ 넷 + 왕관·별뿐.

**패스 4 재평가: 9/10.**

### 패스 5 — 디자인 시스템 정합 (시작 3/10)

따로 둔 디자인 시스템 문서가 없다. **이 문서의 §4 토큰 표를 디자인 시스템으로 삼는다**(자동 확정, 이슈 12=A) — 개인 툴에 별도 시스템 문서는 과하고, `OfficeColors` 가 이미 코드 쪽 단일 소스다. 새 컴포넌트는 인박스 블록·범례 줄·정적 대상 칩·클러스터 배지 넷이고 전부 기존 어휘(배지·카드·탭) 안이다.

**패스 5 재평가: 8/10** — 별도 디자인 시스템 문서 대신 §4 표 + `OfficeColors` 로 대체. 남은 2점은 토큰이 코드(`OfficeColors`)와 문서 두 곳에 있다는 것(T40 에서 코드 주석으로 §4 를 가리켜 묶는다).

### 패스 6 — 반응형·접근성 (시작 2/10)

데스크탑 전용(모바일은 v2 별도 클라이언트)이지만 창 크기 3종과 키보드가 비어 있었다. (자동 확정, 이슈 13=A · 14=A)

**창 크기:**
| 창 | 사무실 폭(패널 480 기준) | 규칙 |
|---|---|---|
| 최소 1100×640 | 620 | scale 하한 **0.6**(0.45 → 0.6), 넘치면 **세로 스크롤** |
| 1280×720(기본) | 800 | 4열, scale ≈0.9 |
| 1920×1080 | 1440 | scale 1.0, 클러스터 두 개가 한 줄에 들어가면 **나란히**(폭 합 ≤ 사무실 폭) |
| 2560×1440 | 2080 | scale 상한 **1.5**, 폭 ≥1600 이면 **5열** |
- 사무실은 **세로 스크롤 영역**(부장 카펫·클러스터), 내 책상 + 범례는 **바닥 고정 바**(높이 120 + 24)로 스크롤에서 분리. 문은 스크롤 영역 왼쪽에 고정.
- 오른쪽 패널 폭 **드래그 420~720, 기본 480**(앱 로컬 저장). **터미널 탭을 열면 660 으로 자동 확장**(80열 × D2Coding 13px ≈ 7.8px/자 = 624 + 패딩 24 + 스크롤바 12), 다른 탭으로 가면 원래 폭. `Ctrl+T` = 터미널 전체 폭 오버레이 토글(사무실 위를 덮음, Esc 로 닫힘).
- 픽셀아트는 레이아웃 scale 과 **분리**: `spriteScale = scale >= 0.75 ? 2 : 1`(정수 배율만), `FilterQuality.none`. 캐릭터·소품·왕관은 이 배율, 텍스트·선은 레이아웃 scale.

**키보드·접근성:**
- `Enter` 전송 · `Shift+Enter` 줄바꿈(현행) · `Ctrl+K` 지시 바 포커스 · `Ctrl+L` 로그 탭 · `Ctrl+T` 터미널 · `Ctrl+I` 지시문 · `Ctrl+R` 보고서 · `Esc` 부장 선택으로 복귀.
- 인박스 맨 위 카드에 `Alt+Y` 허가 / `Alt+N` 거부(Alt 조합 — 터미널 탭 타이핑과 충돌 방지). 카드에 단축키 힌트 글자.
- 포커스 링 2px `#FFFFFF` 알파 0.8 — 브라우저/Flutter 기본 대신 팔레트로. 스크롤바 6px 팔레트 색.
- 대비: 본문 ≥13px, 보조 글자 `#8A93A8` on `#1B1F2A` ≈ 5.6:1(통과), 모니터 초록 `#9BE7A1` on `#10141D` ≈ 12:1. 범례 7칸은 색만이 아니라 **아이콘/링 모양**(점선·❗·⏳·⚠)으로도 구분(색약 대응).
- 클릭 목표: 캐릭터 히트 반경 `charRadius + 4` 최소 18px, 슬롯·배지 최소 24px, 버튼 32px.

**패스 6 재평가: 9/10.**

### 패스 7 — 미결 설계 결정 등록부

| 결정 | 자동 확정(권장안) | 미루면 생기는 일 |
|---|---|---|
| Flame 전환 여부 | **CustomPainter 유지** — 스프라이트는 `drawImageRect` 아틀라스, 애니메이션은 기존 `office_motion` 틱. Flame 은 씬 그래프·카메라가 필요할 때(v2 장식·줌) 재검토 | 렌더러 두 벌 |
| 스프라이트 셀·포즈 | **32×32 셀, 아틀라스 8열**, 포즈 8종: idle(앉음) · type(2프레임 0.5s) · think(고개 기울임) · alert(❗ 손 들기) · ask(❓) · report(📋 서류) · error(고개 숙임) · gone(의자만). 걷기 4프레임(좌/우). 13상태→8포즈: 작업→type, thinking→think, 한가/보고대기→idle, 내 차례→alert, 대기→ask(ask_parent)/idle+⏳(셸락·starting), 오류→error, 퇴근→gone, 보고 방문→report | T33 이 매번 되묻는다 |
| 에셋 출처·라이선스 | **CC0 우선**: Kenney(CC0) 16×16 캐릭터·타일을 2× 로 쓰고 포즈는 직접 편집. 유료·CC-BY 는 LICENSE 기록 조건으로만. **AI 생성 스프라이트는 쓰지 않는다**(프레임 간 일관성·라이선스 불명). 서체 3종 OFL | 라이선스 불명 에셋이 저장소에 들어온다 |
| 캐릭터 외형 | 엔진별 셔츠 색(Claude 주황 `#E0956E`, Codex 파랑 `#8FB4FF` — 현 배지 색) + 이름 해시로 머리색 4종. 직급은 왕관/별 배지로만 | 8명이 똑같아 보인다 |
| 부서 탭 아이콘 | 폴더 아이콘 + 이름, 툴팁 cwd(현행) | — |
| 클러스터 색 배정 | 팀 생성 순서로 6색 순환, 팀 id 해시 아님(같은 세션에서 안정) | 재접속마다 색이 바뀐다 |
| 퇴근 접기 타이머 | 10분, 앱 로컬 | — |
| 말풍선 상시 표시 | alert 만(패스 4) | 말풍선 밭 |

**미결(사용자 결정 대기 없음)** — 전부 자동 확정. 실제 사용 후 수정은 04 에 D-## 로 append.

## 4. 범위 밖

- 모바일 클라이언트 레이아웃 — v2 별도 앱(01 §Distribution).
- 사무실 장식(식물·정수기·게시판 소품) — T33 에셋에 여유가 있으면, 없으면 v2. 상태 전달에 기여하지 않으면 그리지 않는다(빼기 원칙).
- 줌·팬(카메라) — 세로 스크롤로 충분한지 사용 후 판단. 필요해지면 Flame 재검토.
- 다크/라이트 테마 전환 — 다크 단일(사용 장면: 밤·듀얼 모니터 옆 창).
- 사운드(보고 도착 알림음) — 사용 후 판단.
- 보고서 마크다운 렌더링(표·코드 하이라이트) — 텍스트 + 코드 고정폭까지만.

## 5. 이미 있는 것 (재사용)

§1 표 그대로. 추가로: 이동 애니메이션(`office_motion`)이 출근·복구 연출의 뼈대, `PendingInbox` 위젯이 인박스의 뼈대, `RankBadge` 가 왕관/별, `redo_card` 가 만료 카드, `member_gone_banner` 가 오류 재고용.

## 6. 구현 태스크

이 리뷰의 결정에서 나온 것만. 작업계획 M6 에 **T40(레이아웃 v2 구현)** 을 추가하고 T33 은 스프라이트로 좁힌다.

- [ ] **T40-1 (P1, human: ~1d / CC: ~40min)** — 사무실 캔버스 — 세로 스크롤 + 내 책상·범례 바닥 고정 바, scale 0.6~1.5, 5열, 클러스터 폭 = min(n,4)열·왼쪽 정렬·한 줄 나란히
  - 나온 곳: 패스 6 창 크기 · 패스 2 빈 상태(1명 팀 전폭 상자)
  - Files: `dev/app/lib/office/office_layout.dart`, `office_view.dart`
  - Verify: 레이아웃 단위 테스트(1/4/8팀 × 3창 크기 — 겹침 0, 스크롤 높이), 캡처
- [ ] **T40-2 (P1, ~1d / ~40min)** — 페인터 — 팀 카펫 6색 토큰·제목 태그, 부장 금색 카펫, 책상 라벨(이름+배지, 축소 시 숨김 순서), 모니터 2줄, 범례 7칸 + 13→7 매핑, 오류 시 책상 테두리 빨강, 퇴근 의자만
  - 나온 곳: 패스 1 앵커·글자 빼기 · 패스 2 매핑표 · 패스 4 토큰
  - Files: `office_painter.dart`, `office_scene.dart`
  - Verify: 페인터 골든/시맨틱 테스트, 캡처
- [ ] **T40-3 (P1, ~1d / ~40min)** — 내 책상·다중 대기 — 슬롯 4칸 + "+N", `visitorSpot(desk, k)` 최대 2 + "+N", 슬롯 클릭 = 선택 + 인박스 스크롤
  - 나온 곳: 패스 2 이슈 5
  - Files: `office_layout.dart`, `office_scene.dart`, `office_view.dart`
  - Verify: 동시 ask_parent 2명·허가 6건 테스트
- [ ] **T40-4 (P1, ~1d / ~40min)** — 오른쪽 패널 — 전역 인박스 블록(2장 펼침, +N), 허가 카드 요약 줄·3줄 클램프·주 버튼 2·위험 태그·만료 메타, redo 카드 인박스 안으로, 보고서 탭 문서 흐름 + 미확인 배지, 패널 폭 드래그 420~720·터미널 자동 660·Ctrl+T 오버레이
  - 나온 곳: 패스 1 이슈 2 · 패스 3 이슈 8·9 · 패스 4 하드리젝션 ① · 패스 6 패널 폭
  - Files: `panel/right_panel.dart`, `pending_card.dart`, `report_tab.dart`, `terminal_tab.dart`, `main.dart`
  - Verify: 위젯 테스트(선택 무관 인박스, 배지, 폭 전환), 캡처
- [ ] **T40-5 (P2, ~4h / ~20min)** — 상단 바·지시 바·오버레이 — 데몬 pill 3상태 + 진행 바, 예외 "자세히" 접힘, 부서 탭 폴더 아이콘, "보고 N", 정적 칩 "♛ 부장에게", placeholder 예시, 단축키(Ctrl+K/L/T/I/R, Alt+Y/N, Esc), 포커스 링·스크롤바 팔레트
  - 나온 곳: 패스 2 이슈 6 · 패스 1 이슈 3 · 패스 6 키보드
  - Files: `topbar/top_bar.dart`, `command/command_bar.dart`, `main.dart`
  - Verify: 위젯 테스트, 캡처
- [ ] **T40-6 (P2, ~4h / ~20min)** — 빈 상태·로딩 연출 — 부서 0 큰 버튼, 부장만 점선 자리, 출근 걷기 1.2초 + "(출근 중)", 복구 점선 3초 + `↻ 복구됨`, 퇴근 10분 뒤 "퇴근 N" 접기
  - 나온 곳: 패스 2 이슈 6·7 · 패스 3 스토리보드
  - Files: `office_scene.dart`, `office_motion.dart`, `office_painter.dart`
  - Verify: 상태 테스트, 캡처
- [ ] **T33 (P2, ~2d / ~60min)** — 스프라이트 — Kenney CC0 기반 32×32 아틀라스 8포즈 + 걷기, 서체 3종(Galmuri11·Pretendard·D2Coding) 동봉 + LICENSE, `spriteScale` 정수 배율·`FilterQuality.none`, 13→8 포즈 매핑, 왕관·별·의자 소품
  - 나온 곳: 패스 7 등록부
  - Files: `dev/app/assets/`, `pubspec.yaml`, `office_painter.dart`
  - Verify: 1×/2× 배율 캡처에서 흐림 없음, 포즈 8종 시맨틱 테스트

_패스 5(디자인 시스템)에서 새로 나온 태스크는 없다._ — §4 표가 시스템이다.

## 7. 완료 요약

| 패스 | 점수 | 남긴 것 |
|---|---|---|
| 1 · 정보 구조 | 4/10 → 9/10 | 시선 서열, 앵커, pending 의 주인 하나 |
| 2 · 상태 커버리지 | 3/10 → 9/10 | 상태 13종 → 범례 7칸 매핑표 |
| 3 · 사용자 여정 | 4/10 → 9/10 | 첫 부서부터 첫 보고까지 8단계 스토리보드 |
| 4 · AI 슬롭 위험 | 5/10 → 9/10 | 하드리젝션 2건 해소(보고서 카드 스택 · 오버레이 예외 문자열) |
| 5 · 디자인 시스템 정합 | 3/10 → 8/10 | §4 토큰 표 = 단일 소스, `OfficeColors` 와 묶기 |
| 6 · 반응형·접근성 | 2/10 → 9/10 | 세로 스크롤 + 바닥 고정 바, 패널 폭 420~720, 터미널 660 |
| 7 · 미결 결정 등록부 | — | 8건 해소, 이월 0 |

**총점 5/10 → 9/10.** 결정 14건(대화 12 + 자동 확정 2), 이월 0. 목업 3종 중 A 를 큰 틀로 골랐다.
범위 밖 6건과 "이미 있는 것" 은 §4·§5 에 적어 두었다.

사용자 지시(2026-09-17): 이슈 9 이후는 권장안(10/10) 자동 선택. 실제 사용 후 수정은 04 에 append.

## 승인된 목업

| 화면 | 목업 | 방향 | 비고 |
|---|---|---|---|
| 사무실 전체 레이아웃 | variant-A | 색 카펫 클러스터 + 내 책상 슬롯 + 하단 범례 + 80열 터미널 | 큰 틀. 부장 방은 일반 크기(D3) |
| 혼합 요소 | variant-B · variant-C | B: 모니터 2줄·전역 허가 블록·제목=카펫 색 / C: 대기 수 배지·폴더 탭·오류 테두리 | B 의 임의 멤버 지시·멤버 목록 패널은 제외 |

## 결론

레이아웃 v2 확정 — **T40**(레이아웃 구현)과 **T33**(스프라이트·서체)으로 넘긴다. 미결 결정은 없다.
독립적인 2차 의견은 다른 모델의 서브에이전트 하나로만 받았다(Codex 는 계정 사용량 한도).
아키텍처·테스트 쪽 검토는 이 프로젝트에서 작업계획과 `PROTOCOL.md` 가 대신해 왔다.
